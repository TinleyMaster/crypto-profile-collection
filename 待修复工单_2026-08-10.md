# 待修复工单 — crypto-profile-collection

> 整理时间：2026-08-10 14:11
> 来源：五轮只读审计（测试/审计由二狗完成，本工单交给负责改 bug 的 AI 执行）
> 数据库：`postgresql://root:***@43.166.198.83:32405/crypto`
> 源码：`github.com/TinleyMaster/crypto-profile-collection`（已 clone 到 `source/`）
> **总原则**：所有"数据已在库里只是没解析/没标注"的修复均为**零 API 成本**；动手前建议先 `pg_dump` 一份当前库。

---

## P0 — 阻断性 / 数据正确性根因 / 最高风险

### FIX-001 数据库连接池打爆（已造成 20+ 分钟全站不可用）

- **现象**：`FATAL: sorry, too many clients already`，直连与所有 `/api/*` 全挂，首页壳 12.8s。
- **根因**：`scripts/src/crypto_research/db/conn.py` 的 `get_connection` **每次新建连接、无连接池**，带 5 次重试 + 3/6/9/12s 退避；`task_manager` 允许 **3 任务并发**，而脚本内线程池叠加——deep_doc_discovery `--workers` 默认 **20**、retro_scan_spa **10**、spa_browser_crawl **8**，`_flush_db()` 在 worker 侧各自开连接 ⇒ 峰值几十条连接打爆上限。
- **修复方案**：
  1. `conn.py` 改用 `psycopg_pool.ConnectionPool(max_size=5)`，全局单例复用。
  2. 调低 worker：`deep_doc_discovery --workers 4`、`retro_scan_spa 4`、`spa_browser_crawl 4`。
  3. `task_manager` 给 `max_concurrent × workers` 设总量上限（如 ≤ 12）。
  4. 应急：重启 Zeabur 服务释放连接。
- **成本**：改代码（Python）。**风险**：中（连接逻辑改动，建议先单测）。
- **影响**：消除全站不可用风险。

### FIX-002 单点存储无备份（唯一"不做就归零"的风险）

- **现象**：2.1 GB 数据全在一台 Zeabur 实例，无任何异地副本。
- **修复方案**：加每日/每周 `pg_dump` 任务，落本地或对象存储（COS/S3）。示例：
  ```bash
  pg_dump "postgresql://root:***@43.166.198.83:32405/crypto" -Fc -f /backup/crypto_$(date +%F).dump
  # 保留最近 7 份，旧份同步到对象存储后删本地
  ```
  建议用 Zeabur 的 Cron / 外部 GitHub Action 定时触发。
- **成本**：加一个备份脚本 + 定时任务。**风险**：低。
- **影响**：崩溃可回滚，避免几个月沉淀归零。

### FIX-003 白皮书 entry_type 被 CHECK 约束禁止（数据正确性根因）

- **现象**：全库 `entry_type='whitepaper'` = **0 条**，不是少标，是数据库拒绝写入。
  `chk_doc_source_entry_type` 只允许 11 个值，**没有 `whitepaper`**，同样缺席 `audit`/`forum`/`explorer`/`blog`。
- **修复方案**（三步，零 API 成本，纯读 `raw.api_response`）：
  ```sql
  -- 步骤1：补类目
  ALTER TABLE biz.doc_source_entry DROP CONSTRAINT chk_doc_source_entry_type;
  ALTER TABLE biz.doc_source_entry ADD CONSTRAINT chk_doc_source_entry_type
  CHECK (entry_type = ANY (ARRAY[
    'official_website','docs','docs_portal','github','medium','announcement',
    'twitter','telegram','reddit','facebook','other',
    'whitepaper','audit','forum','explorer','blog'
  ]::varchar[]));

  -- 步骤2：用 CoinGecko ground truth 重标已在库的 1,880 条
  WITH cg AS (
    SELECT DISTINCT ON (payload->>'id')
      payload->>'id' AS cg_id,
      NULLIF(payload->'links'->>'whitepaper','') AS wp
    FROM raw.api_response WHERE endpoint_code='coin_info'
    ORDER BY payload->>'id', fetched_at DESC
  ), w AS (
    SELECT m.asset_id,
      regexp_replace(regexp_replace(lower(cg.wp),'^https?://(www\.)?',''),'/+$,'') AS wpn
    FROM cg JOIN core.asset_source_map m
      ON m.source_code='cg' AND m.source_asset_key=cg.cg_id
    WHERE cg.wp IS NOT NULL
  )
  UPDATE biz.doc_source_entry e
  SET entry_type='whitepaper', updated_at=now()
  FROM w
  WHERE e.asset_id = w.asset_id
    AND regexp_replace(regexp_replace(lower(e.entry_url),'^https?://(www\.)?',''),'/+$,'') = w.wpn;

  -- 步骤3：插入库外 3,511 条
  INSERT INTO biz.doc_source_entry (entity_type, asset_id, source_code, entry_type, entry_url, discovered_from)
  SELECT 'asset', w.asset_id, 'cg', 'whitepaper', w.wp, 'cg_links_whitepaper'
  FROM w
  WHERE NOT EXISTS (
    SELECT 1 FROM biz.doc_source_entry e
    WHERE e.asset_id = w.asset_id
      AND regexp_replace(regexp_replace(lower(e.entry_url),'^https?://(www\.)?',''),'/+$,'') = w.wpn
  )
  ON CONFLICT DO NOTHING;
  ```
  > ⚠️ 步骤 2/3 里 `'/+$'` 在本文档被转义显示，实际 SQL 请写 `regexp_replace(..., '/+$', '')`（去掉反斜杠）。
- **成本**：零 API，纯 SQL。**风险**：低（只改标注/加行，不动现有链接）。
- **影响**：TOP500 白皮书覆盖率从"标注 0%"补到宽口径 81.6%；`whitepaper`/`audit`/`forum`/`explorer`/`blog` 五类可正常入库。

### FIX-004 doc_source_entry 表膨胀（约 50 万死元组）

- **现象**：估算 ~72 万行 vs 实际 22.4 万行 ⇒ 约 50 万死元组未回收；所有分析查询变慢。
- **修复方案**：
  ```sql
  VACUUM (FULL, ANALYZE) biz.doc_source_entry;
  -- 之后建议对该表分析查询统一走"全表 GROUP BY 预聚合"（见审计方法论），相关子查询会退化成反复全表扫描
  ```
- **成本**：纯运维，建议在低峰期跑（FULL 会锁表）。**风险**：中（锁表期间写阻塞，挑维护窗口）。
- **影响**：查询性能提升一个数量级，统计 `pg_stat` 数字才可信。

---

## P1 — 数据质量 / 分类正确

### FIX-005 GitHub 漏标（与白皮书同一个病）

- **现象**：URL 含 `github.com` 的有 **26,532** 条，标成 `github` 仅 **5,270**，漏标 **21,313（80.3%）**。
- **修复方案**（用 raw 层 `repos_url.github` 基准重标，零 API 成本，精确匹配）：
  ```sql
  WITH cg AS (
    SELECT DISTINCT ON (payload->>'id')
      payload->>'id' AS cg_id,
      payload->'links'->'repos_url'->'github' AS gh_arr
    FROM raw.api_response WHERE endpoint_code='coin_info'
    ORDER BY payload->>'id', fetched_at DESC
  ), gh AS (
    SELECT m.asset_id,
      regexp_replace(regexp_replace(lower(g.url),'^https?://(www\.)?',''),'/+$,'') AS ghn
    FROM cg JOIN core.asset_source_map m
      ON m.source_code='cg' AND m.source_asset_key=cg.cg_id
    CROSS JOIN LATERAL jsonb_array_elements_text(cg.gh_arr) AS g(url)
    WHERE cg.gh_arr IS NOT NULL
  )
  UPDATE biz.doc_source_entry e
  SET entry_type='github', updated_at=now()
  FROM gh
  WHERE e.asset_id = gh.asset_id
    AND regexp_replace(regexp_replace(lower(e.entry_url),'^https?://(www\.)?',''),'/+$,'') = gh.ghn
    AND e.entry_type <> 'github';
  ```
  （可选）库外缺失的 GitHub URL 用同样 CTE 做 `INSERT ... WHERE NOT EXISTS`，与 FIX-003 步骤 3 同构。
- **成本**：零 API，纯 SQL。**风险**：低。
- **影响**：GitHub 覆盖从 5,270 回到 ~26,532，`other`(68,471) 假性膨胀大幅回撤。

### FIX-006 市值 / 排名 / 分类解析入 core.asset（排序层基础）

- **现象**：`core.asset` 仅 9 字段，无市值/排名/分类；11 张行情表全 0。但 `raw.api_response` 有 18,078 条 CG `/coins/{id}` 完整 JSON，`payload->>'id'` 100% 对上 `core.asset_source_map`(cg)。
- **可提取字段**（当前入库全 0）：市值排名 13,406 / 赛道分类 18,056 / 流通量 18,060 / 关注人数 18,078 / ATH 16,861 / 市值USD 16,851。
- **修复方案**：写一次性迁移 SQL，从 `raw.api_response` 解析 JSONB `payload` 写入 `core.asset` 新增列（market_cap_usd / market_cap_rank / category / circulating_supply / watchers / ath_usd）。关联键必须用 `payload->>'id'`（**不是** `entity_key`，它全 NULL）。
- **成本**：零 API，纯 SQL 迁移。**风险**：低（新增列，不改现有数据）。
- **影响**：排序层有了抓手，后续可只砸前 1,000 币，成本降一个数量级；可建 T0/T1/T2 关注池。

### FIX-007 每日 asset_market_daily 快照（补时间维度）

- **现象**：全库只有当前快照，raw 仅 3 天（8/4、8/5、8/8）。能答"现在是什么"，答不了"变成了什么"。
- **修复方案**：在采集流水线末尾加一步，每天把 `core.asset` + `raw.api_response` 的市值/价格快照落 `asset_market_daily`。**越早开始越值钱，不可追溯补齐**。
- **成本**：改采集流水线（加一个落库步骤）。**风险**：低。
- **影响**：支持变化检测(diff) 推荐引擎（见系统演进建议，比 AI 相关性评分更靠谱）。

### FIX-008 三个空 symbol 资产 + 资产缺 lifecycle

- **现象**：3 个资产 `canonical_symbol` 为空；`core.asset.status` 全为 `active`，无生命周期（下架/迁移不自知）。
- **修复方案**：
  1. 用 `core.asset_source_map` / `coin_basic` 回填空 symbol。
  2. `core.asset` 加 `lifecycle` 列（active/delisted/migrated），采集时据 CMC/CG 状态同步。
- **成本**：纯数据修复 + 加一列。**风险**：低。
- **影响**：工作台搜索/展示不再出现空 symbol；能识别已死项目。

---

## P2 — 增强 / 已写好未启用模块

### FIX-009 启用高价值未启用模块（代码现成，从没跑）

- **现象**：`scripts/bin/collect_github_activity.py` → `biz.github_repo_activity`=0；`scripts/bin/curate_notebooklm.py` → `biz.doc_source_notebooklm`=0；`biz.asset_unlock_event`=0（有设计文档）。
- **修复方案**：把这三个接入 `task_manager` 的 TASK_DEFS（当前 17 项里没有）。注意 `curate_notebooklm.py` 是单资产按需触发+缓存，与已砍的 B5 全量预跑不同，建议保留。
- **成本**：改调度配置 + 可能少量适配。**风险**：低。
- **影响**：解锁时间表（预测抛压唯一资料）、GitHub 活跃度时间序列、NotebookLM 策展 三个信号层补齐。

### FIX-010 raw.api_response 保留期 + 入库端每资产条目上限

- **现象**：`raw.api_response` 221 MB 且只增不删；"重置高条目资产"是事后补救。
- **修复方案**：
  1. `raw.api_response` 设保留期（如保留最近 30 天，旧快照归档/删除）。
  2. 入库端对单资产 `doc_source_entry` 设条目上限（如 200），从源头防膨胀，替代事后重置。
- **成本**：改采集/入库逻辑。**风险**：低。
- **影响**：库增长可控，避免再次表膨胀。

---

## 附：执行顺序建议

1. **先做 FIX-002（备份）** —— 任何写库操作前先留后路。
2. **FIX-001（连接池）** —— 不修的话跑 FIX-003/005 的批量 UPDATE 可能再次打爆。
3. **FIX-004（VACUUM FULL）** —— 低峰期，提升后续所有操作性能。
4. **FIX-003 + FIX-005** —— 分类正确性，零 API，收益最高。
5. **FIX-006 / 007 / 008** —— 排序层 + 时间维度，支撑推荐引擎。
6. **FIX-009 / 010** —— 增强与长效治理。

> 所有 SQL 中的 `'/+$'` 转义提示见 FIX-003 注释；关联 CG 一律用 `payload->>'id'` 而非 `entity_key`。

# CM 链上指标 · 主流币历史极值分位 — 执行计划

> 工单 ID：FEAT-CM-ONCHAIN-PCTL
> 创建日期：2026-08-29
> 状态：待手动执行

---

## 执行步骤

### Step 1: 筛选达标主流币

```bash
cd /Users/tinley/项目/代码/crypto-profile-collection/05_代码与脚本/scripts/bin

python filter_cm_major_coins.py --output cm_major_coins.json
```

**预期输出**：`cm_major_coins.json`（达标币种清单，含历史长度、非空率）

---

### Step 2: 建表

```bash
cd /Users/tinley/项目/代码/crypto-profile-collection/05_代码与脚本/scripts

psql $DATABASE_URL -f sql/biz/cm_asset_onchain_daily.sql
```

**验证**：`\d biz.cm_asset_onchain_daily` 可见 `source_cutoff` 列与索引

---

### Step 3: 入库

```bash
cd /Users/tinley/项目/代码/crypto-profile-collection/05_代码与脚本/scripts/bin

# 从清单文件入库
python ingest_cm_onchain_daily.py --list cm_major_coins.json

# 或指定币种
python ingest_cm_onchain_daily.py --coins btc,eth
```

**验证**：
```sql
SELECT COUNT(*), COUNT(DISTINCT cm_symbol) FROM biz.cm_asset_onchain_daily;
SELECT MAX(metric_date), MAX(source_cutoff) FROM biz.cm_asset_onchain_daily;
```

---

### Step 4: 创建分位视图

```bash
cd /Users/tinley/项目/代码/crypto-profile-collection/05_代码与脚本/scripts

psql $DATABASE_URL -f sql/biz/cm_onchain_percentile.sql
```

**验证**：
```sql
SELECT * FROM biz.cm_onchain_percentile_full WHERE cm_symbol = 'btc' ORDER BY metric_date DESC LIMIT 5;
SELECT * FROM biz.cm_onchain_percentile_roll365 WHERE cm_symbol = 'btc' ORDER BY metric_date DESC LIMIT 5;
```

---

### Step 5: 验证

```bash
cd /Users/tinley/项目/代码/crypto-profile-collection/05_代码与脚本/scripts/bin

python validate_cm_onchain.py
python validate_cm_onchain.py --symbol btc
```

---

## 验收标准对照

| 验收项 | 对应步骤 |
|---|---|
| M-1 输出达标主流币清单 | Step 1 |
| M-2 映射后 asset_id 命中率 100% | Step 3（无映射会 WARN） |
| M-3 表建成，source_cutoff 列可见 | Step 2 |
| M-4 入库行数与清单一致，MAX(metric_date)='2026-05-24' | Step 3 |
| M-5 BTC MVRV 极值抽查通过 | Step 5 |
| dropna 生效，NULL 不参与分位 | Step 5 |
| py_compile + dry-run 通过 | 已验证（代码已提交） |

---

## 已知限制

- **数据冻结 2026-05-24**：所有分位截至该日，非实时
- **CC BY-NC 4.0**：非商用许可，商业化需另行授权
- **asset_id 映射**：首次入库需 `core.asset` 中已有对应币种（BTC/ETH 必有，Tier-2 需先 bootstrap）

---

## 文件清单

| 文件 | 用途 |
|---|---|
| `scripts/bin/filter_cm_major_coins.py` | M-1 筛选脚本 |
| `scripts/sql/biz/cm_asset_onchain_daily.sql` | M-3 建表 DDL |
| `scripts/bin/ingest_cm_onchain_daily.py` | M-4 入库脚本 |
| `scripts/sql/biz/cm_onchain_percentile.sql` | M-5 分位视图 |
| `scripts/bin/validate_cm_onchain.py` | M-7 验证脚本 |

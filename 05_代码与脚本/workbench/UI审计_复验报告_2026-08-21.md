# UI 审计报告 · 修复复验（2026-08-21）

> 复验角色：二狗（审计/验证位，不动代码、不碰 git）
> 复验方式：静态代码交叉核对 + git 历史核对 + `ast.parse` 语法校验
> 对照基线：`UI审计_report_2026-08-21.md`（4 个问题：1×P1 + 2×P2 + 1×P3）

---

## 一、复验结论速览

| 问题 | 级别 | 修复是否到位 | 关键证据 |
|---|---|---|---|
| P1 导航双 tab 同锚点 | 🔴 | ✅ 到位 | index/kol/research 三处均无「总览」，6 tab 对 6 section |
| P2a 全量限流内存全局→文件持久化 | 🟡 | ✅ 到位 | `_FEED_FULL_TS_FILE`+`_RecrawlFileLock`+原子写 `os.replace` |
| P2b 前端 429 不退避 | 🟡 | ✅ 到位 | 读 `retry_after*1000` + backoff×2 + `Math.max(wait, pollInterval×backoff)` |
| P3 增量漏日志 | 🟢 | ✅ 到位 | 后端 SQL `>=` + 前端 `(task_id,line_no)` Set 去重（双保险） |

**总评：4 个问题全部修复到位，逻辑闭环，`app.py` 语法校验通过（APP_SYNTAX_OK）。**

---

## 二、逐项复验

### P1 — 导航 7 tab 对 6 section ✅
- **index.html:2631-2637**：`资产库/行情/解锁/热力/回测/任务` 6 个 `data-section` tab + 1 个 `/kol` 跨页链接；无「总览」。
- **kol.html:514-520 / research.html:1426-1432**：同 6 个 `/#xxx` tab + KOL（active），无「总览」。
- **一致性**：`index.html:5719` 的 `querySelectorAll('.top-nav .nav-tab[data-section]')` 抓 6 个 tab，对应 6 个 `section`（assets/market/unlock/heat/backtest/tasks，见 2643/2795/2832/2852/2869/2897），**无孤儿、无双高亮**。
- ⚠️ **女王大人汇总笔误提醒**：汇总里写「保留 7 个有效 tab」，实际代码是 **6 个**（删「总览」后剩 6）。代码本身正确，仅表述与代码不符，不影响功能。

### P2a — 全量限流跨 worker 文件持久化 ✅
- **定义（app.py:78-79）**：`_FEED_FULL_TS_FILE`、`_FEED_FULL_TS_LOCK` 均在 `RECRAWL_STATE_DIR`（67-72）**之后**定义，与 `recrawl_state`/`unlock_state` 同目录同模式。
- **读（82-89）**：`_load_feed_full_ts()` 读 json 返回 `ts`，异常兜底 `0.0`。
- **写（92-96）**：`_save_feed_full_ts()` 用临时文件 + `os.replace` **原子写**，比直接覆盖更稳。
- **限流段（730-740）**：`if since_ts_val is None` → `_RecrawlFileLock` 加锁 → 读 `last_ts` → `<5.0s` 返回 `429`+`retry_after` → 否则 `_save_feed_full_ts(now)`。多 worker 现在共享同一文件，限流真正生效。

### P2b — 前端 429 退避 ✅
- **index.html:5776-5782**：429 分支 `var wait = body.retry_after * 1000`（秒→毫秒，**单位正确**）→ `backoff=min(backoff*2,8)` → `timer=setTimeout(..., Math.max(wait, pollInterval*backoff))`。
- 不前进 `latestTs`（避免丢数据），下轮按退避调度，**限流契约闭环**。

### P3 — 增量去重双保险 ✅
- **后端（app.py:756）**：`WHERE tl.created_at >= to_timestamp(%s)`（已改 `>=`）。
- **前端（5751 / 5823-5825）**：`seenKeys:new Set()`，在 `appendSchedulerLogs` 插入前按 `task_id+':'+line_no` 去重。
- 双保险，原「严格 `>` 漏同微秒并发日志」风险消除。

---

## 三、Git 历史核对（重要提醒）

- 女王大人所述 commit **`5e07510` 确实存在**，message = `fix(ui): 审计修复-P1删冗余总览tab-P2全量限流跨worker-P3增量去重` ✅
- **但 `5e07510` 不是当前 HEAD**。当前 HEAD = `493032d`，其上还多了 3 个 commit：
  - `3112fb3` fix(kol): 修复 db.py 语法错误（SQL 注释写在 Python 字符串外导致模块导入失败）
  - `e62833c` fix(crash): 修复 **app.py 启动崩溃**——全量限流变量定义在 `RECRAWL_STATE_DIR` 之前导致 `NameError`
  - `493032d` fix(db): 全面压缩连接数
- **解读**：`5e07510` 初版引入了一个**启动崩溃**（P2 文件持久化时把 `_FEED_FULL_TS_FILE` 定义放到了 `RECRAWL_STATE_DIR` 之前 → `NameError`），女王大人自己发现并用 `e62833c` 修了；顺手还修了 db.py（`3112fb3`）。现在 `RECRAWL_STATE_DIR` 在前、`_FEED_FULL_TS_FILE` 在后（67→78），崩溃已消除。
- 所有 commit 均已 push（main 分支，含 `remotes/origin/main`）。
- 📌 **给女王大人的提醒**：你说「已推送（commit 5e07510）」，实际最终落地是 **`493032d`**，中间多了 3 个修复 commit。代码全在、已推送，但汇报时 commit 号口径需对齐。

---

## 四、非阻塞观察项（不影响运行，仅供优化参考）

1. **`app.py:711` `global _feed_full_last_ts` 是死声明**
   原内存全局变量已删除，仅剩这个 `global` 声明、函数体里也无人读写该名。无害（当前不报错），但会误导后续维护者以为有 `_feed_full_last_ts` 状态变量。建议删除该行。

2. **`seenKeys` Set 只增不减**
   `appendSchedulerLogs` 从头部裁剪超 `maxLines`(500) 的旧行时（5843-5845），被删行的 key 仍留在 `seenKeys`。因 `task_log.line_no` 不重复，功能上**无误删新日志**，仅长期运行内存缓慢累积，量级为日志行数级，可接受。

---

## 五、复验总结

4 个问题（P1 + P2×2 + P3）**全部修复到位，逻辑自洽，无新增回归**。修复过程有过一次启动崩溃（NameError）但已自修，最终状态健康，`app.py` 语法校验通过。两处非阻塞观察项供后续顺手清理。审计位复验闭环完成，未改动任何源码、未触碰 git 远程。

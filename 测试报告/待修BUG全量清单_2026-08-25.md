# 待修 BUG 全量清单 · 2026-08-25（截至 12:34）

> 审计纪律：全部结论来自 prod DB 只读复验（git + code + prod DB + 线上 runtime 四交叉）。
> 标注口径：`✅闭环` = 实跑复验确认已修复；`❌未闭环` = 仍有残留/未生效；`⏳待修` = 已定位未动手；`🔶待定` = 需主人决策。

## 一、P0 / P1 核心项（已闭环 ✅）

| 项 | 复验结论 | 关键证据 | 闭环点 |
|---|---|---|---|
| **P0-1 supply 跨源不一致** | ✅ 闭环 | 偏差>10% 仅 **1**（BABYBNB）；反向清零 **0**；USDF 确为 CMC 上游退化非 bug | source_map 清僵尸 is_primary(68行)+唯一索引 `uq_srcmap_asset_src_primary_true` |
| **P1 rank 缺口** | ✅ 闭环 | `market_cap>0` 缺口 **0**、10,316 资产连续无断层 rank；备份表 `public._bak_rank_null_20260825` 存在 | 全站统一重排（注：dev 口径"有市值"=`market_cap>0`，mc=0/Null 不计入） |
| **source_map 治理** | ✅ 闭环 | 重复 is_primary 组数 **0**；唯一索引真实存在 | 清僵尸映射+部分唯一索引 |
| **85 组同名重复(dedup)** | ✅ 闭环（本次重跑推翻"待确认"） | `symbol+name` 完全相同组 **0**（a5ade5e 复验时 85 → 现 0） | 全库 `dedup_assets.py --apply` 已清 |
| **symbol 重复（不同 name）** | — 非 bug | 1,616 组合理撞车（包装/跨链币） | 不动 |

## 二、当前仍挂着的待修项（按优先级）

| 优先级 | 项 | 状态 | 复验结论（prod/线上） | 下一步 | 验证状态 |
|---|---|---|---|---|---|
| **P1·高** | **7843276 行情分区可见性 · 线上未生效** | ❌ 未闭环 | 线上 API 仍是老查询：BTC 行情 **3 天**、VOOx/RCATx **0 天**、correlation `common_days=2`；已证线上连 prod 同库（API 值逐位吻合） | dev 在 zeabur 容器 `git pull` 含完整合并逻辑的 `db_stats.py` 并**触发重构建部署** | git+code+DB ✅ / **runtime ❌** |
| **P2·低** | **ANALYZE 从未跑** | ⏳ 待修 | `pg_stat` 全 0（`last_analyze=NULL`）；`doc_source_entry` 真实 292,858 行、死元组 ~50 万未回收 | 全库 `ANALYZE` + 大表 `VACUUM` + 定时化（工单已出） | DB 实证 ✅ |
| **P1·独立** | **KOL classifier.py 解析偏差** | 🔶 待 fix | `post_type` 准确率 55.6%、`PREDICTION` 漏标 100%、`NOISE` 误标 30.6%（前瞻喊单全归 analysis、生活/营销当信号） | 根因已定位（prompt 保守偏见 + 无 noise 类 + 压力/支撑无独立字段）；**待主人确认 GT 边界后转 fix 工单** | 离线测试台 ✅ |

## 三、非 bug · 仅知悉（分类缺口 / 数据范围）

| 项 | 说明 |
|---|---|
| 白皮书标注仅 16.4% | 是 URL 多混在 docs/other 的**分类缺口**，非采集缺口（约束已修好） |
| cmc_historical 回填偏窄 | 仅 506 资产、VOOx/RCATx 仅覆盖近 2 周——回填脚本日期范围问题，与 7843276 查询修复无关 |
| 日价 42% 覆盖 | 设计取舍（TOP 资产优先），非缺陷 |
| 数据库连接池无池 / doc_source_entry 禁用相关子查询 | 运维隐患（每次新建连接×5 重试；22.4万行须预聚合）— 见运维清单 |

## 四、运维隐患（独立，可并入 P2 或单独排期）

| 项 | 现状 | 修法 |
|---|---|---|
| 连接池无池 | 每次新建 + 5 次重试退避，并发×workers 易打爆 | `psycopg_pool.ConnectionPool(max_size=5)` + 降 workers |
| doc_source_entry 相关子查询 | 22.4 万行 / 1.8GB / 50 万死元组，相关子查询超时 | 预聚合 TEMP TABLE + JOIN |

## 五、二狗建议收敛顺序

1. **先掐 7843276 zeabur 重部署**（最影响用户、代码已就绪、纯部署动作）→ 重部署后二狗再验（达标线：BTC≥90 天、correlation≥30 天）
2. **P2 ANALYZE 工单执行**（无风险、秒级、立竿见影）
3. **KOL 解析**：等你确认 GT 边界，转 fix 工单
4. 运维两项并入下一轮排期

## 六、本次交付物

- `复验结论_85组同名重复_dedup_2026-08-25.md`（dedup 闭环确认）
- `工单_P2_ANALYZE未跑_2026-08-25.md`（P2 可执行工单）
- `复验结论_行情分区_线上重部署复验_2026-08-25.md`（上一轮 7843276 runtime 未生效）
- 待修全量清单（本文件）

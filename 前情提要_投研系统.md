# 投研笔记本系统 — 前情提要与技术上下文

## 一、项目背景

我已有两套系统：

**采集系统（已有，不动）**
- 仓库：`TinleyMaster/crypto-profile-collection`
- 部署：Zeabur（Docker + PostgreSQL）
- 职责：自动采集加密货币的投研资料（官网文档、白皮书、审计报告、链上数据、代币经济学、解锁数据等）
- 对外暴露：Flask REST API（`/api/assets/search`, `/api/assets/<id>/materials`）

**投研系统（新系统，你要搭建）**
- 仓库：新建独立仓库
- 部署：同样是 Zeabur（Docker + 共用同一个 PostgreSQL）
- 职责：类似 Google NotebookLM，每个代币创建一个"笔记本"，导入采集系统收集的文档链接作为来源，AI 对话时所有回复基于来源并标注出处

## 二、页面与交互流程

### 首页 `/`
- **搜索框**：居中大搜索框，输入代币名称实时搜索（调采集系统 API）
- **搜索结果**：下拉显示匹配的代币列表（symbol + name），选中后出现「创建笔记本」按钮（注意：此时不需要显示该资产底下的文档列表，直接一键创建）
- **一键创建**：点击后后台创建笔记本 → 自动跳转到 `/notebook/{notebook_id}`
- **已有笔记本列表**：首页下方显示已创建的所有笔记本卡片（代币名、创建时间、来源数），点击卡片进入对应笔记本
- **再次访问首页**：刷新后直接看到已有笔记本列表，搜索框仍在顶部

### 笔记本页 `/notebook/{notebook_id}`
完全模仿 NotebookLM 布局：

- **左侧面板（~300px）**：
  - 笔记本标题（代币名 + symbol）
  - 「添加来源」按钮 → 弹窗展示该代币在采集系统中的文档列表（从 `doc_source_entry` 读取），勾选后导入
  - 已导入的来源列表（URL 标题、类型标签、状态）
- **右侧主区域**：
  - 对话区（类 ChatGPT 气泡）
  - AI 回复包含脚注式引用标注 `[1]` `[2]`，hover 显示来源标题和链接，点击跳转原文
  - 底部输入框
- **顶部导航**：左上角「← 返回首页」链接

## 三、架构设计

```
┌──────────────────────────┐     ┌─────────────────────────────┐
│  采集系统（已有）           │     │  投研系统（新）               │
│  crypto-profile-collection │     │                             │
│                          │     │                             │
│  采集层：CMC/CG/DL/官网   │     │  notebooks 表               │
│  doc_source_entry (文档)  │←读取│  sources → chunks           │
│  asset_tokenomics         │     │  pgvector 向量索引            │
│  asset_token_unlocks      │     │  DeepSeek RAG 对话           │
│  asset (代币基准)          │     │                             │
└──────────────────────────┘     └─────────────────────────────┘
         同一个 PostgreSQL 数据库
```

## 四、已确定的技术选型

| 层 | 选型 |
|---|------|
| 后端框架 | Python FastAPI |
| 数据库 | PostgreSQL（共用采集系统的 PG 实例） |
| 向量扩展 | **pgvector**（PG 扩展，零额外运维） |
| Embedding | **DeepSeek API** |
| RAG 对话 | **DeepSeek Chat**，所有回答基于来源文档并标注引用 |
| 文档加载 | 从采集系统 `biz.doc_source_entry` 表读 URL → 爬取内容 → 清洗分块 |
| 前端 | 单页 HTML/JS，两个路由（首页 + 笔记本页），类 NotebookLM 风格 |
| 前端路由 | 可用简单的 hash 路由或路径参数，不需要重型框架 |
| 部署 | Zeabur Docker，监听 `$PORT`（Zeabur 自动注入） |

## 五、需要新创建的数据表

建议用独立 schema `notebook` 隔离：

```sql
CREATE SCHEMA IF NOT EXISTS notebook;

-- 笔记本表
CREATE TABLE notebook.notebooks (
    notebook_id BIGSERIAL PRIMARY KEY,
    asset_id BIGINT NOT NULL UNIQUE,          -- 关联采集系统 core.asset.asset_id
    asset_symbol VARCHAR(128) NOT NULL,
    asset_name VARCHAR(256) NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 来源文档表
CREATE TABLE notebook.sources (
    source_id BIGSERIAL PRIMARY KEY,
    notebook_id BIGINT NOT NULL REFERENCES notebook.notebooks(notebook_id) ON DELETE CASCADE,
    entry_id BIGINT,                           -- 可选：关联采集系统 biz.doc_source_entry.entry_id
    url TEXT NOT NULL,
    title VARCHAR(512),
    content_text TEXT,                         -- 清洗后的纯文本
    content_length INT,
    chunk_count INT DEFAULT 0,
    fetch_status VARCHAR(16) DEFAULT 'pending', -- pending/fetching/fetched/failed
    fetch_error TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 文档分块表（带向量）
CREATE TABLE notebook.chunks (
    chunk_id BIGSERIAL PRIMARY KEY,
    source_id BIGINT NOT NULL REFERENCES notebook.sources(source_id) ON DELETE CASCADE,
    chunk_index INT NOT NULL,                  -- 第几个 chunk（从 0 开始）
    content TEXT NOT NULL,                     -- chunk 文本
    embedding VECTOR(1536),                    -- 确认 DeepSeek embedding 实际维度后调整
    token_count INT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_chunks_embedding ON notebook.chunks USING ivfflat (embedding vector_cosine_ops);

-- 对话消息表
CREATE TABLE notebook.messages (
    message_id BIGSERIAL PRIMARY KEY,
    notebook_id BIGINT NOT NULL REFERENCES notebook.notebooks(notebook_id) ON DELETE CASCADE,
    role VARCHAR(16) NOT NULL,                 -- user / assistant
    content TEXT NOT NULL,
    citations JSONB,                           -- [{"source_id": 1, "title": "...", "url": "...", "excerpt": "..."}]
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

## 六、后端 API 设计

```
# === 首页 ===
GET  /api/assets/search?q=xxx          → 搜索代币（直接透传采集系统 API）
GET  /api/notebooks                     → 获取所有笔记本列表

# === 笔记本 ===
POST /api/notebooks                     → 创建笔记本 { asset_id, asset_symbol, asset_name }
GET  /api/notebooks/{id}                → 笔记本详情（含来源列表）
DELETE /api/notebooks/{id}              → 删除笔记本

# === 来源文档 ===
GET  /api/notebooks/{id}/available-sources  → 该代币在采集系统中可导入的文档列表（从 doc_source_entry）
POST /api/notebooks/{id}/sources            → 批量导入来源 { entry_ids: [1,2,3] }
POST /api/notebooks/{id}/sources/{sid}/fetch → 爬取单个来源内容 + 向量化
POST /api/notebooks/{id}/fetch-all          → 一键爬取所有来源
DELETE /api/notebooks/{id}/sources/{sid}    → 删除来源（同时删 chunks）

# === 对话 ===
POST /api/notebooks/{id}/chat           → 对话 { message } → { answer, citations }
GET  /api/notebooks/{id}/messages       → 获取对话历史
```

**注意**：创建笔记本时不需要传递文档列表，笔记本创建后再由用户手动添加来源。

## 七、采集系统数据库表结构（你需要读取的表）

### 7.1 core.asset — 资产主表

| 字段 | 类型 | 说明 |
|------|------|------|
| `asset_id` | BIGSERIAL PK | 自增主键 |
| `canonical_symbol` | VARCHAR(128) NOT NULL | 代币符号，如 `OP` |
| `canonical_name` | VARCHAR(256) NOT NULL | 资产名称，如 `Optimism` |
| `asset_type` | VARCHAR(32) | token/coin/stablecoin 等 |
| `status` | VARCHAR(32) | active/inactive/deprecated |
| `launch_date` | DATE | 发布日期 |

### 7.2 biz.doc_source_entry — 文档来源表（核心数据源）

| 字段 | 类型 | 说明 |
|------|------|------|
| `entry_id` | BIGSERIAL PK | |
| `asset_id` | BIGINT FK → core.asset | 关联资产 |
| `entity_type` | VARCHAR(16) | `asset` 或 `protocol` |
| `entry_type` | VARCHAR(32) | official_website, whitepaper, audit, docs, github, blog, twitter, telegram, medium 等 |
| `entry_url` | TEXT NOT NULL | 文档链接 |
| `source_code` | VARCHAR(32) | cmc/cg/manual/deep_crawl 等 |
| `is_primary` | BOOLEAN | 是否主链接 |
| `ai_noise_checked_at` | TIMESTAMPTZ | NULL=未检查，有值=AI 判定为投研相关 |
| `needs_browser` | BOOLEAN | SPA 页面标记 |
| `spa_crawled_at` | TIMESTAMPTZ | SPA 爬取时间 |

**查询可导入文档的关键 SQL：**

```sql
SELECT entry_id, entry_url, entry_type, source_code, is_primary
FROM biz.doc_source_entry
WHERE asset_id = %s
  AND ai_noise_checked_at IS NOT NULL   -- 已被 AI 判定为投研相关
ORDER BY is_primary DESC, entry_type;
```

### 7.3 biz.asset_tokenomics — 代币经济学

| 字段 | 类型 | 说明 |
|------|------|------|
| `asset_id` | BIGINT PK | |
| `total_supply` | NUMERIC | 总量 |
| `max_supply` | NUMERIC | 最大供应 |
| `circulating_supply` | NUMERIC | 流通量 |
| `allocation_json` | JSONB | `[{"category":"Team","pct":5},...]` |
| `emission_schedule` | TEXT | 释放计划 |
| `governance_info` | TEXT | 治理说明 |
| `utility_info` | TEXT | 用途说明 |

### 7.4 biz.asset_token_unlocks — 代币解锁数据

| 字段 | 类型 | 说明 |
|------|------|------|
| `asset_id` | INTEGER PK | |
| `overview_json` | JSONB | 概览（释放进度、市值、FDV、分配明细） |
| `unlock_events_json` | JSONB | 解锁事件数组 |
| `source_url` | TEXT | tokenomist 页面地址 |

### 7.5 core.asset_source_map — 来源映射

| 字段 | 类型 | 说明 |
|------|------|------|
| `asset_id` | BIGINT | |
| `source_code` | VARCHAR(32) | cmc/cg/dex 等 |
| `source_asset_key` | VARCHAR(256) | 来源系统的 asset ID |

## 八、数据流

```
1. 首页搜索 → 调用采集系统 API 搜索代币
2. 选中代币 → 点击创建 → POST /api/notebooks → 跳转笔记本页
3. 笔记本页 → 点击添加来源 → GET available-sources（从 doc_source_entry 读）
4. 勾选来源 → POST sources 批量导入 → 后台爬取内容 → 分块 → 向量化
5. 对话 → embed 问题 → pgvector 搜索 → 拼接上下文 → DeepSeek Chat → 返回答案+引用
6. 后续访问首页 → GET /api/notebooks → 显示已有笔记本卡片
```

## 九、API 契约（采集系统对外接口）

```
# 搜索资产
GET https://crypto-profile-collection.zeabur.app/api/assets/search?q=optimism
→ {"ok": true, "assets": [{"asset_id": 2747, "symbol": "OP", "name": "Optimism", ...}]}

# 获取资产素材（含文档列表）
GET /api/assets/<asset_id>/materials
→ {"ok": true, "data": {"asset": {...}, "doc_entries": [...], "tokenomics": {...}}}
```

如果你的系统和采集系统共用同一个 PG 实例，可以直接连数据库查询，不需要调 HTTP API。如果用 HTTP API，采集系统地址是环境变量 `COLLECTION_API_URL`。

## 十、部署环境

- 平台：Zeabur（自动构建 Docker）
- 数据库：PostgreSQL（Zeabur 托管，连接串在环境变量 `DATABASE_URL`）
- 需要安装：pgvector 扩展（`CREATE EXTENSION IF NOT EXISTS vector;`）
- 环境变量：
  - `DATABASE_URL` — PG 连接串
  - `DEEPSEEK_API_KEY` — DeepSeek API Key
  - `DEEPSEEK_BASE_URL` — `https://api.deepseek.com`
  - `COLLECTION_API_URL` — 采集系统地址（如果用 API 方式读数据）
  - `PORT` — Zeabur 自动注入
- Docker 基础镜像：`python:3.11-slim`
- 用 `gunicorn` 启动 FastAPI，监听 `$PORT`

## 十一、关键提醒

1. **不要做文档管理系统** — 文档的采集、去噪、分类已经在采集系统完成了，你只需要从 `doc_source_entry` 表读取现成的 URL 列表
2. **向量维度** — 先确认 DeepSeek embedding 模型的实际输出维度，建表时 `VECTOR(N)` 的 N 要对齐
3. **分块策略** — 建议 500-1000 token/chunk，50-100 token overlap
4. **引用格式** — AI 回复中必须标注引用来源链接和文档标题，脚注风格 `[1]` `[2]`
5. **笔记本一对一** — 一个 asset_id 对应一个笔记本
6. **来源导入是异步的** — 批量导入后立即返回，后台逐个爬取+向量化，前端轮询或 WebSocket 更新状态
7. **首页无需展示文档列表** — 搜索代币时只显示 symbol + name，创建笔记本后才在笔记本页内添加来源

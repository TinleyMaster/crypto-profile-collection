# 盘面异动扫描系统设计方案

> 版本：v1.0（2026-09-23）
> 日期：2026-09-16 初稿 / 2026-09-23 现行版
> 目标：批量扫描加密代币盘面异动，识别高确定性机会，输出可执行观察清单。
>
> **本文只描述系统的「当前」口径与实现**——不含历史修订记录、不含已被取代的参数值、不含「原实现如何」的对照说明。
> 凡标注「**实测**」的为 prod 只读查询结果；凡标注「**未实现**」的是设计里有但代码里没有的东西。
> 未解决的缺口与待决策项统一收敛在 **§12.1**。

---

## 1. 背景与目标

现有系统已具备：CMB/CMC 行情日线、衍生品资金面快照（funding/OI/CVD 24h 聚合）、催化剂、KOL、解锁、链上监控、恐惧贪婪指数、板块资金流等数据与采集脚本，且已有 `auto_trade_signal.py` 交易执行链路和 `backtest_opportunities.py` 回测框架。

本方案在其之上新增一套**盘面异动扫描系统**，定位为"研究辅助"，输出盘面观察清单与信号，不直接自动下单。

**核心设计原则：**

1. 低成本粗筛优先：全市场先跑价量粗筛，命中者才拉 OI/CVD 做深度校验，控制算力与 API 消耗。
2. 双池并行：主异动扫描池 + 蓄势埋伏观察池，互补漏检。
3. 回测先行：8 个深度校验场景必须经历史回测标定后，才作为信号可信度依据。
4. 数据自积累：OI/CVD/清算等依赖时间序列的指标，从现在开始持续采样入库。

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    数据采集层（定时任务）                      │
│   Binance klines（5m/15m/1h）│ OI 历史序列 │ CoinGlass 爆仓快照 │
│   Binance 多空比（/futures/data/*，按需）                     │
│   现有：asset_derivatives 快照 / fear_greed / market_daily    │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│  L0 市场环境过滤（regime filter）                              │
│   BTC 方向/主导率、恐慌贪婪指数、总市值趋势、板块资金流          │
└─────────────────────────────────────────────────────────────┘
              │                              │
┌─────────────────────────────┐  ┌─────────────────────────────┐
│  主异动扫描池                 │  │  蓄势埋伏观察池               │
│  L1 粗筛：价 + 量 多周期共振   │  │  窄幅震荡 + OI 持续抬升       │
│  L2 校验：P/OI/CVD/VOL 8 场景 │  │  突破放量 → 转入主池          │
└─────────────────────────────┘  └─────────────────────────────┘
              │                              │
              └──────────────┬───────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────┐
│  信号合成 / 去重 / 冷却 / 生命周期                              │
│  叠加催化层加分项（催化剂/KOL/解锁/链上大额转账共振）             │
└─────────────────────────────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────┐
│  输出：盘面观察清单（信号类型/置信度/建议触发价/止损）→ 人工决策   │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 数据层设计（本方案的数据前提）

### 3.1 数据源与免费可用性

| 数据 | 数据源 | 免费？ | 说明 |
|---|---|---|---|
| 分钟级 K 线（5m/15m/1h 全市场） | Binance `GET /fapi/v1/klines`（或 `api/v3/klines`） | ✅ | 实时拉取；现有 `signal_market_data.py` 已在使用 |
| OI 历史序列 | Binance `GET /futures/data/openInterestHist` | ✅ | 免费无 key，周期 5m~1d；**每请求≤500 条、仅保留最近 30 天**，必须自建历史表持续采样 |
| OI 实时快照 | Binance `GET /fapi/v1/openInterest` | ✅ | 单点值，无历史 |
| CVD 日内 | Binance aggTrades 计算 or 各交易所 CVD 接口 | ✅ | 需自建计算/采样；现有 `asset_derivatives` 仅 24h 聚合 |
| 资金费率 | 现有 `asset_derivatives` / `ingest_btc_funding.py` | ✅ | 已有 |
| **爆仓滚动窗口（实际在用）** | CoinGlass v4 `/api/futures/liquidation/coin-list`（HOBBYIST 套餐） | ❌ 付费 | 全币种一次拉取，出 `liq_usd_1h/4h/12h/24h` + 多空分列；**粒度下限 4h**（无细粒度历史），只能 5min 轮询自建序列；实测 1,597 币 → 映射 527 池内币 |
| 爆仓细粒度历史（**未采用**） | CoinGlass `/liquidation/history`（4h+，套餐可用）/ Binance WS `!forceOrder@arr` | 部分 | ⚠️ **两条都没用起来**：WS 订阅脚本存在但 `biz.liquidation_events` 至今 **0 行**（`allForceOrders` 已 404，WS 长期无产出），故爆仓实际只有 coin-list 一条源；4h+ 历史接口可作回测/缺口补数，目前未接入（见 §12.1） |
| 多空比（大户持仓/账户/全体/主动买卖） | Binance 免费 `/futures/data/*` | ✅ | 仅对**跟踪中/候选**币按需拉取，非全市场 |
| 市场环境 | 现有 fear_greed / market_daily / sector_flow_daily | ✅ | 已有 |

### 3.2 需要新建的表

**① OI/CVD 采样历史表 `biz.oi_cvd_snapshot`**（蓄势池与 OI↑↓ 判定的数据前提）

```sql
CREATE TABLE IF NOT EXISTS biz.oi_cvd_snapshot (
    symbol       TEXT           NOT NULL,
    ts           TIMESTAMPTZ    NOT NULL,          -- 采样时间
    exchange     TEXT           NOT NULL DEFAULT 'binance',
    oi_usd       NUMERIC(24,2),                    -- OI 总价值（USDT）
    cvd_5m_usd   NUMERIC(24,2),                    -- 本采样窗口净主动成交（正=主动买盘强）
    cvd_1h_usd   NUMERIC(24,2),                    -- 近 1h 累计 CVD（写后回填）
    vol_5m_usd   NUMERIC(24,2),                    -- 本采样窗口总成交额
    source       TEXT           NOT NULL DEFAULT 'realtime',  -- realtime / backfill（fix_057 加列，fix_059 回标）
    PRIMARY KEY (symbol, exchange, ts)
);
CREATE INDEX IF NOT EXISTS idx_oi_cvd_snapshot_ts ON biz.oi_cvd_snapshot (ts DESC);
CREATE INDEX IF NOT EXISTS idx_oi_cvd_snapshot_sym_ts ON biz.oi_cvd_snapshot (symbol, ts DESC);
CREATE INDEX IF NOT EXISTS idx_oi_cvd_snapshot_source_ts ON biz.oi_cvd_snapshot (source, ts DESC);
```

采样频率建议：每 5 分钟一次（与 5m 周期对齐），5m/1h 的 OI 变化率与 CVD 由历史序列现算，而非依赖 24h 快照。

> **`source` 列的用途（实测）**：区分「5m 实时采样」与「1h 历史回填」。两者**不可混用**——
> 主池算「近 2 桶 OI 变化」只取 `source='realtime'`（回填行会污染）；蓄势池 ACC 需要「连续 ≥12 个小时桶」，
> **刻意不过滤** `source`（回填行正好补停摆/部署窗口的小时桶）。
> 保留期：`OI_CVD_RETENTION_DAYS = 90`（528 币 × 288 桶/天 ≈ **15 万行/天**，90 天 ≈ 1350 万行）。

**② 爆仓滚动窗口快照表 `biz.liquidation_snapshot`**（**轧空扫描实际使用的爆仓源**；另一张 `biz.liquidation_events` 见下方说明）

```sql
CREATE TABLE IF NOT EXISTS biz.liquidation_snapshot (
    symbol            TEXT         NOT NULL,        -- 合约符号（如 BTCUSDT；含中文名合约，CoinGlass 能对上）
    ts                TIMESTAMPTZ  NOT NULL,        -- 采样时间（5min 桶）
    source            TEXT         NOT NULL DEFAULT 'coinglass',
    liq_usd_1h        NUMERIC(24,2),                -- ⚠️ 滚动 1h 窗口快照（非整点累计）
    liq_usd_4h        NUMERIC(24,2),
    liq_usd_12h       NUMERIC(24,2),
    liq_usd_24h       NUMERIC(24,2),
    long_liq_usd_1h   NUMERIC(24,2),                -- 多单爆仓（价格下跌被强平）
    short_liq_usd_1h  NUMERIC(24,2),                -- 空单爆仓（价格上涨被强平）
    long_liq_usd_4h   NUMERIC(24,2),
    short_liq_usd_4h  NUMERIC(24,2),
    fetched_at        TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
    PRIMARY KEY (symbol, ts)
);
CREATE INDEX IF NOT EXISTS idx_liq_snapshot_ts ON biz.liquidation_snapshot (ts DESC);
CREATE INDEX IF NOT EXISTS idx_liq_snapshot_sym_ts ON biz.liquidation_snapshot (symbol, ts DESC);
```

> **消费口径（必须遵守）**：各 `*_liq_usd_*` 列是 CoinGlass 的**滚动窗口累计值**。
> 消费侧**取绝对值**（`liq_usd_1h` = 最近 1 小时爆仓额），**严禁跨桶差分**——相邻两次滚动快照相减得到的是
> 「新滚入 − 滚出」（平稳时≈0、回落时常为负），再 `max(…,0)` 截断即假 0（曾把 25.6 万 U 空头爆仓显示为 `+0.000%`）。
> 迁移与注释见 `scripts/migrations/fix_056_squeeze_scan.sql`。

**②′ 清算事件表 `biz.liquidation_events`（⚠️ 保留但当前 0 行，属「空转通路」）**

```sql
CREATE TABLE IF NOT EXISTS biz.liquidation_events (
    id           BIGSERIAL PRIMARY KEY,
    event_ts     TIMESTAMPTZ    NOT NULL,
    symbol       TEXT           NOT NULL,
    side         TEXT,                             -- SELL/BUY（清算方向）
    qty          NUMERIC(24,8),
    price        NUMERIC(24,8),
    usd_value    NUMERIC(24,2),                    -- 清算名义价值
    exchange     TEXT           NOT NULL DEFAULT 'binance',
    order_id     BIGINT,                           -- 原始订单 id（幂等去重键（symbol, order_id））
    ingested_at  TIMESTAMPTZ    NOT NULL DEFAULT NOW(),
    UNIQUE (symbol, order_id)
);
```

> **实测：本表 `count(*) = 0`**，且 Binance `allForceOrders` 已 404、`!forceOrder@arr` 长期无产出。
> 现状：`supervisord.conf` 中 `[program:liquidation_collector]` **整段注释停用**（保留原配置便于回滚），
> **脚本与表均保留**（`phase_liquidation_collector.py` / `biz.liquidation_events`），若日后 Binance 恢复广播该流可再启用。
> 爆仓维完全由 CoinGlass 滚动快照承接。

**③ 信号输出表 `biz.scan_signal`**（主池 + 蓄势池统一出口，供回测与观察清单消费）

```sql
CREATE TABLE IF NOT EXISTS biz.scan_signal (
    id           BIGSERIAL PRIMARY KEY,
    signal_ts    TIMESTAMPTZ    NOT NULL,
    symbol       TEXT           NOT NULL,
    pool         TEXT           NOT NULL,          -- 'main' / 'accumulation' / 'squeeze'
    scenario     TEXT,                             -- S1..S8 / ACC / BRK / SQZ_<CONCLUSION>
    timeframe    TEXT,                             -- 5m/15m/1h（主池多周期共振级别）
    p_dir        TEXT, price_chg_pct NUMERIC(8,2),
    vol_state    TEXT, vol_ratio      NUMERIC(8,2),
    oi_dir       TEXT, oi_chg_pct     NUMERIC(8,2),
    cvd_dir      TEXT,
    cvd_usd      NUMERIC(20,2),                    -- CVD 窗口净额（fix_060）
    cvd_ratio    NUMERIC(12,6),                    -- 净额 / 同窗口成交额
    funding_rate NUMERIC(12,8),
    confidence   TEXT,                             -- high/medium/low
    context_tags TEXT[],                           -- 市场环境标签（regime）
    detail       JSONB,                            -- 轧空判定 metrics / 结论 / 原因（fix_056）
    trigger_price NUMERIC(24,8),                   -- 入场位 = 触发周期收盘价
    stop_loss_pct NUMERIC(8,2),                    -- 失效位幅度% = 2×ATR(14)/参考价，夹 [8%,20%]
    breakout_px  NUMERIC(24,8),                    -- 延续确认位 = 触发根方向侧极值（fix_061）
    alerted_at   TIMESTAMPTZ,                      -- 后加：告警去重（同币 12h 冷却）
    alert_suppressed_at     TIMESTAMPTZ,           -- 后加（fix_060）：跨池互斥（main⇄squeeze）压制时刻
    alert_suppressed_reason TEXT,                  -- 后加（fix_060）：压制原因
    exec_state   TEXT,                             -- 后加（fix_046）：执行层状态，dry-run/已执行
    executed_at  TIMESTAMPTZ,                      -- 后加（fix_046）：最近一次执行处理时间
    status       TEXT NOT NULL DEFAULT 'active',   -- active/confirmed/expired
    expired_at   TIMESTAMPTZ,
    created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

> 说明：粗筛候选池、蓄势池无需单独建表持久化，作为内存态/临时态处理即可；落库的是最终信号与历史采样。
>
> **`status` 取值口径 = `active / confirmed / expired`**（与列名 `expired_at`、`biz.squeeze_track` 的终态一致）。
> 退出机制：`scan_daemon` 任务 `expire_signals`（30min）把 `pool='main'` 与 `accumulation/BRK` 超 **24h**、
> `accumulation/ACC` 超 **7 天** 的 `active` 置 `expired` 并写 `expired_at = signal_ts + TTL`
> （确定性截止时刻，不写 `NOW()`）。时机语义来源见 §6.3。
> **入场位 / 失效位**：主池与 BRK 生产者均落 `trigger_price`（= 触发周期收盘价）与 `stop_loss_pct`
> （= `2×ATR(14)` / 参考价，夹 `[8%, 20%]`），消费者见 §6.3 第 3 条。
> **`active → confirmed` 延续确认**：`breakout_px` 列（迁移 `fix_061`）+ `scan_daemon` 任务 `confirm_signals`（30min）。
> `breakout_px` = 触发根方向侧极值（与 `trigger_price` 同根，无未来信息、恒在入场价正确一侧）；
> `signal_ts` 后 **6h** 内一根**已收盘** 1h 收盘价越位 ⇒ `confirmed`。
> `confirmed` 是**质量升格**（仍在可动作集合）**而非入场门槛**——依据见 §6.3 第 3 条。

### 3.3 需要新建的采集任务

| 任务 | 周期 | 说明 |
|---|---|---|
| 全市场 K 线拉取 | 每 5m | Binance 全部 USDT 合约（实测 286 币入库），拉 5m/15m/1h 最近 N 根，供 L1 粗筛 |
| OI/CVD 采样入库 | 每 5m | 逐币拉 OI 快照 + 计算 CVD，写入 `oi_cvd_snapshot`（`source='realtime'`） |
| **爆仓滚动窗口轮询** | 每 5m | CoinGlass `liquidation/coin-list` → `liquidation_snapshot`（**当前唯一可用的爆仓源**） |
| 清算事件订阅（**已停用**） | — | `!forceOrder@arr` → `liquidation_events`；**实测 0 行**，`[program:liquidation_collector]` 已注释停用 |
| 多空比按需采集 | 随扫描轮次 | **不是独立任务**——内嵌在 `scan_squeeze` 轮次里（`_fetch_long_short_ratio`）；仅对 `squeeze_track` 中 `tracking` 的币拉 4 个 `/futures/data/*` 端点（≤12 币/轮） |
| 衍生品快照刷新 | 复用现有 | `phase_derivatives_batch.py`（15min 缓存），提供 funding 等；**不在 `scan_daemon.TASK_DEFS` 内** |

> **落地位置（实测）**：以上前 3 项与多空比采集**均在常驻进程 `scan_daemon.py`** 的 **11 个任务**（`TASK_DEFS`）内，
> **不由 `workbench/scheduler.py` 调度**，见 §11.1。

---

## 4. 主异动扫描池（核心管线）

### 4.1 L0 市场环境过滤（新增，必须）

扫描结果脱离市场环境没有意义。只有环境允许时才采信对应方向信号：

- **BTC 方向/主导率**：BTC 近 1h/4h 方向；BTC.D 趋势。
- **恐慌贪婪指数**（已有数据）：极端恐慌时多头信号降权，空头信号升权。
- **总市值趋势**：市场总市值处于上升段才采信多头信号。
- **板块资金流**（已有 `sector_flow_daily`）：标的所在板块净流入时加分。

输出 `context_tags`，随信号落库。

### 4.2 L1 粗筛（多周期通道；⚠️ 设计所称"共振"未实现，见下）

**设计意图**是「升档共振」（多周期方向一致才升级）；⚠️ **线上实现并非共振**——`_l1_screen` 是各周期独立判定、取最高 level，跨周期方向一致性检查**未实现**（见下）：

| 级别 | 条件（**实测阈值，与代码一致**） | 处理 |
|---|---|---|
| 候选（level 1） | 5m 价量异动（价格 ±**1.5%**、量 ≥2× 近 20 根均值） | 触发后被调用侧 `if l1["level"] < 2: continue` **丢弃**（等效"只观察"） |
| 入池（level 2） | **15m 单周期**价量异动（价格 ±**2.0%**、量 ≥2×） | 进入 L2 深度校验（tag `lv2_15m`） |
| 高置信（level 3） | **1h 单周期**价量异动（价格 ±**3.0%**、量 ≥2×，tag `lv3_1h`） | 进入 L2 |

- ⚠️ **1h 阈值「两处不同值」的口径澄清**
  回测 `backtest_scan_scenarios.py` 与独立脚本 `phase_scan_main_pool.py` 用
  **4.0%**，线上 `scan_daemon.py::task_scan_main_pool` 用 **3.0%**。**它们不是同一个量**：
  - **回测口径** = 单周期「1h 单根涨跌幅 ≥ 阈值」即触发（`interval='1h'` 逐根判，无 5m/15m 通道）；
  - **线上口径** = 三周期**各自独立成通道**：`_l1_screen` 遍历 1h/15m/5m，任一周期满足
    `|chg| >= PRICE_THR[iv] and vol_ratio >= 2.0` 即入选并取 `LEVEL_RANK` 最高者；
    调用侧 `if l1["level"] < 2: continue` ⇒ **15m(2.0%) 与 1h(3.0%) 都能单独把币送进 L2**。

  故把线上 1h 改成 4.0 **无法复现**回测 4.0 档的效果：回测里被 4.0 滤掉的「1h 涨 3~4%」样本，
  在线上多数已先被 15m 通道（2.0%）捕获入池，改 1h 几乎不削减那批样本，也拿不到那部分期望提升
  （回测 3.0→4.5 档 24h 净均 +5.13%→+6.66% 是针对**单周期**口径测出来的）。
  ⇒ **线上 1h 阈值保持 3.0**；`4.0` 仅为单周期回测口径，不可直接搬到多周期线上（代码内已加同口径注释）。
  多周期阈值（5m/15m）**没有回测支撑**（见 §12.1）。

- 量能判定用"当前周期成交额 vs 近 N 周期均值"（N = 20），避免用 24h 快照。量比阈值 `VOL_RATIO_THR = 2.0`
  在 §8 第三轮扫参里**几乎无边际贡献**（仅削减信号量），定位是流动性护栏而非质量过滤。
- **触发根选择：优先已收盘条** —— 每个周期的判定根取
  **「最近一根已收盘条（`open_time + 周期时长 <= now`，值稳定 ⇒ 判定可复现），不合格才回退最新
  未收盘条（保持及时性）」**；`_l1_screen` 返回 `bar_idx` 指向被判定那一根，`trigger_price` /
  `breakout_px` / `stop_loss_pct`（含 ATR 窗口）全部锚定到该根。
  依据：`scan_klines` 每 5min UPSERT 覆盖未收盘条 ⇒ 一根条的「终值可用且仍是最新根」窗口仅约 2~5 分钟，
  而主池扫描每 15min 一轮 ⇒ 只看最新根会让「能否看到收盘终值取决于相位」。离线量化（6 天 / 280 币，
  用 5m 重建触发条在 +5/+10/+15 分钟的快照）：真异动条（终值 `|chg|≥2%` 且 `vr≥2.0`）**1719** 根中，
  **53.5% 只有收盘才越阈**（+5min 11.9% / +10min 30.1% / 收盘 53.5% / 条内始终不满足 4.5%）。
  prod 单快照回放：新规则 L1(level≥2) 命中 9 / 旧规则 5，补回 4 条且全部过 L2、不在冷却内，反向 0 条 ⇒ 漏检率 ≈44%。
  该修法**会提升信号量**（属预期，非回归）；去重仍由主池 6h 冷却承担，未新增机制。
  自测：`05_代码与脚本/workbench/test_scan_l1_closed_bar.py`。
- ⚠️ **「三周期方向必须一致才升级」未实现**：`_l1_screen` 对 1h/15m/5m
  **各自独立**判定（`|chg| >= PRICE_THR[iv] and vol_ratio >= 2.0`），把达标的最高 `LEVEL_RANK` 写进 `best`，
  **没有任何跨周期方向一致性检查**；`context_tags` 也只携带**触发的那个周期**的标签
  （`lv{level}_{iv}`，见 `task_scan_main_pool`）。因此：
  - 「5m 单独异动只观察」是靠调用侧 `level < 2` 丢弃实现的；
  - **15m 或 1h 任一达标即可单独入池，不要求彼此同向**——即同一条 1h 级信号，其 15m/5m 可能正好反向。
  真正实现共振需改 `_l1_screen` 并需回测支撑（见 §12.1，本轮不动代码）。
- ⚠️ **「1h 同向 → 信号权重 ×2」未实现**：`scan_signal` 无权重列，1h 共振只以 `context_tags` 里的
  `lv3_1h` 标签体现，不参与置信度计算（置信度规则见 §4.3）。若要真正加权重需先加列/改公式。
- **资金费率不参与 L1 粗筛**：费率是 8h 结算的慢变量，与分钟级异动时间维度不匹配；且行情启动初期费率通常仍处中性区间，作为触发条件会漏掉早期行情。费率仅作为 L2 拥挤度辅助标签。

### 4.3 L2 深度校验（8 场景）

**指标定义（本方案补充口径要求）：**

- P：当前周期价格方向（与 L1 一致）。
- VOL：放量 / 缩量（当前成交额 vs 近 20 周期均值）。
- OI：本周期 OI 变化方向（由 `oi_cvd_snapshot` 序列计算，**不是 24h 快照**）。
  ⚠️ **口径（实测）：`oi_dir` 是二值无阈值**——`OI_RISE_BARS = 2`，取最近 2 桶 OI 对比，涨即 `up`、跌即 `down`。
  prod 实测存在 `oi_chg_pct = +0.06%` 仍被判 `high` 的样本，即 `P↑OI↑` 里的「OI↑」**不含幅度门限**。
  这是**刻意与回测保持一致**（`backtest_scan_scenarios.py` 同为二值），改动需两侧同步 ⇒ 目前无 OI 幅度/加速度分档（见 §12.1-B5）。
- CVD：本周期累计成交量差方向（正 = 主动买盘强）。
- funding（资金费率）：辅助**拥挤度风险标签**，**不参与任何筛选/触发**。8h 结算一次、慢变量；正数 = 多头持仓拥挤、多头持续付息；负数 = 空头持仓拥挤、空头持续付息。仅在 L2 用于评估反转/过热风险。

**场景编号（`scenario` 列）—— 两套编码来源，语义不同，必须按来源理解：**

**① 现役主池生产者 `scan_daemon._compute_l2`（线上唯一在产的口径）**：只用 `(p_dir, oi_dir)` 两维，
**CVD / VOL 不参与判定**（CVD 只落 `cvd_dir`/`cvd_usd`/`cvd_ratio` 供渲染；VOL 采信与否由 L1 量比承担）：

| # | P | OI | 解读 | 信号 |
|---|---|---|---|---|
| S1 | ↑ | ↑ | 价涨 + OI 增 = 新开多仓 | 多头进攻 |
| S2 | ↓ | ↑ | 价跌 + OI 增 = 新开空仓 | 空头扎实 |
| S3 | ↑ | ↓ | 价涨 + OI 减 = 多头平仓 / 空头回补 | 多头减仓 |
| S4 | ↓ | ↓ | 价跌 + OI 减 = 多头止损离场 | 空头兑现 |

**② 独立脚本 `phase_scan_main_pool.py`（未被 daemon 调度，其历史行仍在库内）**：按 `(p_dir, oi_dir, cvd_dir)`
三维落**设计口径的 8 场景**：

| # | P | OI | CVD | VOL | 解读 | 信号 |
|---|---|---|---|---|---|---|
| S1 | ↑ | ↑ | ↑ | ↑ | 现货买盘强 + 合约新开多仓，真实多头进攻 | 多头趋势延续 |
| S2 | ↑ | ↑ | ↓ | ↑ | 现货主动卖，上涨靠合约杠杆，诱多 | 警惕回调（空头候选） |
| S3 | ↓ | ↑ | ↓ | ↑ | 现货砸盘 + 合约新开空单，真实空头 | 空头趋势延续 |
| S4 | ↓ | ↑ | ↑ | ↑ | 现货承接，下跌由合约空头砸出，诱空 | 存在反弹潜力 |
| S5 | ↑ | ↓ | ↑ | ↑ | 合约平仓 + 现货买入，获利了结 | 反弹近尾声 |
| S6 | ↑ | ↓ | ↓ | ↑ | 空头回补，非新多进场 | 修复反弹 |
| S7 | ↓ | ↓ | ↓ | ↑ | 空头止盈平仓，跌势衰竭 | 衰竭信号 |
| S8 | ↓ | ↓ | ↑ | ↑ | 现货承接 + 空头离场，抛压释放 | 见底反弹 |

> ⚠️ **同一编号在两套里的含义相反**（如 `S2`：① = 价跌+OI 增「空头扎实」，② = 价涨+OI 增+CVD↓「诱多」）⇒
> **`scenario` 列的值不能脱离来源单独解读**。故渲染层（`scan_daemon._scenario_label`、`send_scan_signal_brief`）
> 一律**按行自身的原始维度重算编号与文案**，不信任列内编号，保证「文案与该行实际维度一致」。
> 「VOL↓ 一律降级为观察」未实现（无 VOL 维度）。要落地需先补 CVD/VOL 判定并与回测口径同步。

**资金费率拥挤度标签（附加层、不参与触发；编号按上表 ② 的设计口径）：**

| 场景 | 附加费率标签 |
|---|---|
| S1 | 费率持续大幅正 → 多头过热，警惕短期回调；费率中性/小幅正 → 健康多头行情 |
| S2 | 高正费率 → 多头极度拥挤，诱多概率进一步放大 |
| S3 | 费率持续大幅负 → 空头过热，警惕空头回补反弹；费率中性/小幅负 → 健康空头行情 |
| S4 | 高负费率 → 空头极度拥挤，诱空概率进一步放大 |
| S5 | 高正费率叠加 → 多头集中兑现，反转风险升高 |
| S6 | 负费率叠加空头平仓 → 反弹力度更强 |
| S7 | 高负费率叠加空头平仓 → 见底反弹概率提升 |
| S8 | 高负费率加持 → 抛压释放，反弹潜力更大 |

> **定位说明（为什么费率不进 L1 粗筛）：**
> 1. 费率是 **8h 结算一次**的慢变量，而价量/OI/CVD 是分钟级高频数据，时间维度不匹配，不适合做异动触发条件。
> 2. 行情刚启动时费率往往还在中性区间，等行情走一段后才变为大幅正/负；拿费率当触发条件只能抓到行情后半段，错过启动点。
> 3. 费率极端值更多是**风险预警信号**（拥挤/过热），不是异动发生信号。

**判定增强（设计意图，⚠️ 均未实现）：**

1. 叠加资金费率拥挤度：极端正/负费率时放大诱多/诱空概率 —— 目前只有文本标签、无阈值判定（§12.1）。
2. 叠加实时清算：近期同方向大额清算进一步验证杠杆挤压叙事 —— 爆仓维无历史、未接入（§12.1）。
3. **OI 二阶信息**：OI 是加速抬升还是减速 —— 无分档（§12.1-B5）。
4. **CVD 状态切换**：CVD 由负翻正（或反之）单独标记 —— 未实现。

**置信度分级（依据 §8 标定）：**

> 标定依据（§8）：仅 P↑OI↑ 组合稳定正期望（24h 净 +5.24%、盈亏比 2.01）；P↓\* 空头侧全部负期望；1h 短窗口被成本吃光。**多头优先、空头默认降级。**

**实际公式（`task_scan_main_pool`，与上文标定一致）**：

```text
if   direction == "up"   and oi_dir == "up":  confidence = high if regime.long_fav  else medium
elif direction == "up":                       confidence = medium                  # P↑OI↓
elif oi_dir == "up":                          confidence = high if regime.short_fav else medium  # P↓OI↑（空头侧对称通道）
else:                                         confidence = medium if regime.short_fav else low    # P↓OI↓
```

- 高：`P↑OI↑` **且** L0 环境顺风（`long_fav`）。这里的「多周期共振」在实现里只是 `level ≥ 2`，
  即**「15m 或 1h 任一周期达标」**——**既不要求 5m 参与，也不要求 1h 共振**（实测 high 里 `lv2_15m` 占多数；见 §4.2）；
  「窗口取 4h/24h」在信号表**无对应字段**，不参与分级（窗口只是回测口径，见 §8）。
- 中：`P↑OI↓`（24h 才转正）；以及 `P↑OI↑` 但 L0 不顺风时降为中。
- 低/降级：**只有 `P↓OI↓`** 在 `short_fav` 为假时降为 `low`；`P↓OI↑` 为假时仍是 `medium`、为真时可直接 `high`
  （实测存在 `P↓OI↑ high`，regime 由 BTC 1h/恐慌贪婪/总市值三项共同判定），"空头默认不预警"靠**告警层只推 high**这一层兜住，
  不是靠 L2 不出信号。`P↑OI↓` 恒为 `medium`，不进告警。
- 告警层只看 `confidence='high'` 且 `pool='main' OR (pool='accumulation' AND scenario='BRK')`（§6）。
- 待验证：CVD 维拆分无历史源，需 WebSocket 流式积累后补标定；样本仅 21 天，需随 OI 窗口拉长复验（§12.1-A1/A2）。

---

## 5. 蓄势埋伏观察池（旁路）

**触发条件（全部满足）：**

1. 价格波动小：**仅判 1h**（最近一根 1h 收盘涨跌幅 `|chg1h| < PRICE_QUIET_THR`=1.5%；实现里**不判 15m**）。
2. 成交量无明显放大：成交额 < 近 20 周期均值 1.5 倍。
3. **OI 连续多周期持续上行**：基于 `oi_cvd_snapshot` 历史序列，如近 12 个 1h 周期 OI 抬升占比 ≥70% 且累计变化 >5%。
4. （可选）同步跟踪资金费率，观察蓄势阶段多空是否提前拥挤——仅作观察，不作为进出池条件。

**增强判定（可选：同步抓取资金费率，观察蓄势阶段多空是否提前拥挤）：**

- 叠加 funding 区分吸筹/派发：
  - OI↑ + funding 接近 0 或小幅正 → 多空均衡吸筹，偏多头潜力；
  - OI↑ + funding 大幅负 → 空头持续建仓，存在逼空潜力；
  - OI↑ + funding 大幅正 → 多头拥挤，警惕派发。
- 蓄势时长记录：进入蓄势池的时间戳，超时（如 7 天）未突破自动出池。

**转主池触发：**

- 后续出现放量（量比 ≥3×）+ 价格突破（突破蓄势区间上/下沿），自动转入主池 L2 校验，标记 `pool='accumulation', scenario='BRK'`。
- BRK 的候选集 = **近 7 天内** `status='active' AND scenario='ACC'` 的币（即 ACC 落库后 7 天内一直具备转 BRK 资格）。
- **ACC 冷却与去重**：`ACC_COOLDOWN_H = 24` —— 冷却期内已有 active ACC 的币不再重复入池，返回值加
  `acc_skipped_cooldown`。若无此冷却，同一币只要仍满足 ACC 条件就会每轮（30min）插入新行
  （实测未加冷却时累计 **807 行只有 123 个不同币**，约 6.6× 重复）。
  §6 第 2 条的「同币冷却 6h」**只在主池落地**（`_in_cooldown_main`），蓄势池靠本条独立冷却。

---

## 6. 信号合成：去重 / 冷却 / 生命周期 / 催化层加分

1. **去重与升级**：同币 5m/15m/1h 多周期触发时，保留最高级别一条，其余挂起为佐证。
2. **冷却**：同币信号触发后冷却 N 小时，冷却期内只记录不重复预警。
   主池 `_in_cooldown_main`（`--cooldown-h` 默认 **6h**）；蓄势池 ACC `ACC_COOLDOWN_H = 24`（§5 末条）；
   告警层另有一道 **12h** 冷却（`COOLDOWN_H=12`）。
3. **生命周期**：`active → confirmed（越过 breakout_px）→ expired（超时）`。
   `scan_daemon` 任务 `expire_signals`（30min）把超期的 `active`/`confirmed` 置 `expired` 并写 `expired_at`：

   | 池 / 场景 | TTL | 依据 |
   |---|---|---|
   | `main`（S1..S8） | 24h | §8 结论 3「1h 窗口全档位期望 <0.6%（成本吃光），24h 为主窗口 → 警报语义即『24h 持有观察』」 |
   | `accumulation` BRK | 24h | 同上（BRK 是行动信号，与主池同窗口） |
   | `accumulation` ACC | 7 天 | 与 `ACC_BRK_CANDIDATE_DAYS=7` 对齐——BRK 候选集要求「近 7 天内 active 的 ACC」，提前过期会让这批币永远等不到突破判定 |

   两处消费侧均筛 **`status IN ('active','confirmed')`**：`_load_alert_candidates`、
   `phase_execute_scan_signal.load_candidates`（后者另加 `--require-confirmed`，默认关）。
   轧空池的 `confirmed` 行不参与（它记的是「已判定事件」，有独立的 `biz.squeeze_track` 状态机）——
   `expire_signals` 的 `pool` 条件天然排除。
   **入场 / 失效位**：主池与 BRK 两条生产者路径均落 `trigger_price`（= 触发周期收盘价）与
   `stop_loss_pct`（= `2×ATR(14)` / 参考价，夹 `[8%, 20%]`），渲染层与执行层均已消费。
   **`confirmed`（延续确认）**：`breakout_px` = **触发根方向侧极值**（`up→high_px`/`down→low_px`，与
   `trigger_price` 同根 ⇒ 无未来信息、且因 `high ≥ close ≥ low` 恒在入场价正确一侧）；
   `confirm_signals`（30min，错峰 480s）判定「`signal_ts` 后 **6h** 内存在一根**已收盘** 1h 条的收盘价越位」，
   升格为 `confirmed`。BRK 直接落 `confirmed`（本身就是突破事件，不走 `confirm_signals`）。
   `confirmed` 是**质量升格**（仍在可动作集合、告警加徽章），**不是入场门槛**——
   回放显示「等越位再入场」会把 24h 期望从 +1.56% 做低到 **+1.36%**（越位位离入场价中位 2.31%），
   与催化层 d3「等价格确认=追高」同源；W=6h 未越位组 24h 仍 **+1.62% 正期望**，故**不提前作废**。
   ⚠️ 该结论的样本仅 **90 条 / 3 个交易日 / 单边上涨 regime**（§12.1-A1）。
4. **催化层共振（展示层，不参与打分）**：盘面信号与以下既有数据共振时，在告警邮件中并列展示——
   - 催化剂事件（`biz.catalyst_impact` 正/负向）
   - KOL 信号（`kol_auto_trade_log` / KOL 监控）
   - 代币解锁压力（`unlock_watchlist`）
   - 链上大额转账监控（`onchain_transfer_log`）
   **实现现状（实测）**：共振**只在告警时点计算**（`task_scan_alert` 内查 catalyst/KOL，窗口 `CATALYST_DAYS=KOL_DAYS=7`），
   **不落库、不进入 `scan_signal` 的任何评分字段** —— 即「共振加分」未落地，实际是「告警邮件里带上下文」（§12.1）。

**双层告警：**

> 时序分层依据：解锁/链上转账等**领先型**信息先于盘面（可提前埋伏）；KOL/突发新闻等**滞后型**信息后于盘面（仅作确认）。因此告警分两层，盘面触发时两类信号碰撞。

| 层 | 脚本 | 机制 |
|---|---|---|
| 事件预置层（先行） | `phase_build_event_watchlist.py`（每 6h，scheduler 注册） | 未来 14 天解锁（`unlock_watchlist`）+ 近 7 天链上大额转账 ≥$10M（`onchain_transfer_log`，过滤稳定币/封装/质押噪音）→ `biz.event_watchlist` |
| 盘面触发层 | **`scan_daemon.py` 的 `task_scan_alert`**（每 5 分钟，错峰 +3min） | 新 high 置信主池信号（P↑OI↑ 标定最优组合）或蓄势 BRK high → 共振核查（事件预置=埋伏型 / 催化剂+KOL=确认型）→ 实时邮件，同币 12h 冷却，`scan_signal.alerted_at` 去重（`NEW_WINDOW_MIN=20` 分钟窗口） |

> 交付渠道：**实时邮件**；`send_scan_signal_brief.py` 不在调度内（仅保留脚本备查）。
> 盘面触发层的运行入口是 `scan_daemon.py` 内的 `task_scan_alert`（常驻进程，见 §11）。

---

## 7. 过滤规则与系统盲区

1. **流动性过滤（量化阈值）**：24h 成交额下限 ≥ **500 万 USDT**（实现在 `_get_24h_quote_volume()` +
   `--min-vol-usd 5000000`，对 K 线/OI/轧空三条采集与扫描统一生效）。刷量/小盘代币直接剔除。
   **尚未实现的另两项过滤**：`asset_liquidity` 表过滤、价差（spread）过滤——全仓无引用（见 §12.1）。
2. **缩量场景**：无论组合如何，只标记不预警。
3. **资金费率定位**：仅做拥挤度辅助判断，**不作为筛选触发条件**，避免过滤掉行情早期启动机会。
   （实现细节：`_lookup_funding` 走「符号候选序列」，未命中写 **NULL 不写 0**，避免"无数据"被当成"费率为零"。）
4. **数据源口径**：CVD 明确现货 or 合约口径，跨交易所归一化（`cvd_ratio`）；避免误读。
   ⚠️ **已知跨源不一致（代码注释已自陈，仅披露未修正）**：轧空的爆仓比例分子是 **CoinGlass 全交易所**爆仓额、
   分母是 **币安** 24h 成交额 → 比例**系统性偏高**，所有 `*_LIQ_RATIO` 阈值必须按此混合口径理解与标定。
5. **盲区声明**：
   - 消息面冲击、解锁事件、链上大额转账 → 由催化层共振覆盖，但该层数据本身有滞后
     （且如上 §6.4：只在告警邮件里展示，不参与打分）。
   - funding 结算点（00/08/16 UTC）前后 OI/CVD 噪声大，扫描结果标记该时段。
   - **爆仓只有「滚动窗口快照」一种粒度**（CoinGlass 套餐下限 4h，只能 5min 轮询；WS 通路 0 行），
     故"某分钟爆了多少"不可知，「实时清算验证」只能靠代理指标（见 §12.1）。
   - **采集允许缺桶**：5m 采样在停摆/部署窗口会断行，实测近 6h 覆盖率 = 5m K 线 72/72、OI 实时 41/72、
     爆仓 30/72。因爆仓列是滚动窗口（每行自包含），**缺桶不污染指标**，消费侧取「最近一条 ≤ 容差」即可；
     但会漏掉恰落在缺口的极值（见 §10.9）。
6. **合规声明**：所有指标仅为盘面数据分析参考，不构成投资建议。

---

## 8. 回测验证方案（上线前置步骤）

**原则：场景划分是假设，回测后才算结论。**

> 本节的 `S1..S8` 是**回测/设计口径**的 8 场景编号（含 CVD 维），与线上主池生产者 `scan_daemon._compute_l2`
> 的 4 场景编号**不是同一套**（同一编号在两套里语义可相反）——两套口径见 §4.3。

1. 数据准备：用 `oi_cvd_snapshot` 自积累 + Binance 30 天 OI 历史回填，生成回测数据集；K 线用 Binance 历史 klines 回填。
   ⚠️ **爆仓维无历史**（`liquidation_snapshot` 只有 2026-09-21 起的自积累、CoinGlass 套餐的细粒度历史不可得）
   → 「实时清算验证」只能靠代理指标；若要补真爆仓维，可用 `liquidation/history`（4h+，套餐可用，见 §12.1）。
2. 对场景逐一统计：**胜率 / 平均盈亏比 / 期望值 / 样本量**（1h/4h/24h 三窗口，净收益扣双边 taker 成本 0.1%）。
3. 消融：baseline（价量触发）vs 叠加 OI 方向的 P×OI 分桶 → 量化 OI 边际增量。
4. 横截面去相关：按入场日聚类，报告独立天数 + 日级 t 统计。
5. 输出：场景标定表，作为 L2 置信度分级的最终依据（脚本 `backtest_scan_scenarios.py`）。

**P1 首轮标定结果（2026-09-16，191 符号，OI 窗口 21 天，净收益已扣成本）：**

| 场景 | n | 1h 净均 | 4h 净均 | 24h 净均 | 24h PF | 日 t(1h) |
|---|---|---|---|---|---|---|
| BASELINE_ONLY | 4959 | -0.186% | -0.121% | +0.733% | 1.14 | -1.78 |
| **P↑OI↑（S1/S2）** | 889 | +0.274% | +1.200% | **+5.239%** | **2.01** | +1.23 |
| P↑OI↓（S5/S6） | 391 | -0.093% | +0.147% | +1.964% | 1.43 | -0.69 |
| P↓OI↑（S3/S4） | 417 | -0.848% | -0.993% | -2.254% | 0.67 | -1.60 |
| P↓OI↓（S7/S8） | 325 | -0.769% | -0.942% | -1.989% | 0.60 | -4.35 |

**消融（Δ vs 基线，1h）：** P↑OI↑ **+0.46%** / P↑OI↓ +0.09% / P↓OI↑ -0.66% / P↓OI↓ -0.58%

**标定结论：**

1. **OI 维度存在边际增量**：P↑OI↑ 24h 较基线提升 +5.43%，消融方向一致，非白做功。
2. **样本期内仅多头侧有正期望，空头侧全部负期望** → 置信度分级改为多头优先、空头默认降级（见 §4.3）。⚠️ 样本为单边上涨 regime，**不可外推为「空头侧无效」**（§12.1-A2）。
3. **窗口越长越优**（24h ≫ 1h），印证成本吃光短窗口。
4. **局限**：样本仅 21 天（单一 regime 快照）、t 值偏弱（见 §12.1-A1）、CVD 维未测（S1/S2、S7/S8 拆分）。

**P1 第二轮 · funding 消融（2026-09-17，191 符号，funding 历史已回填 ~333 天 8h 结算，min-n=15）：**

| 场景 | 费率 | n | 1h 净均 | 4h 净均 | 24h 净均 | 24h PF | 日 t(24h) |
|---|---|---|---|---|---|---|---|
| BASELINE_ONLY | +（正） | 901 | -0.131% | -0.101% | -0.362% | 0.95 | -0.56 |
| BASELINE_ONLY | 0（近零） | 2863 | -0.083% | +0.159% | +1.519% | 1.35 | +1.50 |
| BASELINE_ONLY | -（负） | 440 | -0.292% | -0.664% | -0.574% | 0.91 | -0.51 |
| **P↑OI↑** | **+（正）** | **181** | **+0.945%** | **+3.079%** | **+5.962%** | **1.87** | +1.28 |
| P↑OI↑ | 0（近零） | 588 | -0.098% | +0.329% | +2.133% | 1.49 | +1.60 |
| P↑OI↑ | -（负） | 152 | +0.737% | +2.717% | **+15.582%** | **3.42** | +0.38 |
| P↑OI↓ | + | 78 | -0.266% | +1.812% | -0.196% | 0.97 | +0.29 |
| P↓OI↑ | 0 | 270 | -0.877% | -1.165% | -1.907% | 0.66 | -1.56 |
| P↓OI↓ | + | 50 | -1.514% | -3.371% | -8.655% | 0.24 | -2.45 |

**funding 消融结论：**

1. **"高费率做多更差"仅对裸追涨成立**：baseline 在正费率下 24h 转负（-0.36%），近零费率才勉强转正（+1.52%）→ 若只按价量追涨，正费率确实是危险信号。
2. **对 P↑OI↑ 主场景不成立（关键反转）**：无论费率正/负/近零，24h 全部正期望；正费率下反而更好（+5.96%，PF 1.87）——P↑OI↑ 本身已含动量确认，正费率是强趋势伴随而非拥挤反转。
3. **P↑OI↑ + 负费率 24h +15.58%（PF 3.42）但 t 仅 0.38、n=152**：更像空头挤压反弹，样本小、离散大，不构成可靠策略，仅作观察。
4. **空头侧（P↓OI*）所有费率桶 24h 全负**，再次确认空头默认降级。
5. **工程结论**：funding **不加入** P↑OI↑ 的硬性过滤（避免误杀正期望）；可作为加分项（置信度 +1）保留在 `context_tags` 展示，供人工判断。

**P1 第三轮 · 阈值敏感性扫描（2026-09-17，191 符号，45 天 K 线窗口，min-n=15，聚焦 P↑OI↑ 24h）：**

价格阈值对期望是主杠杆，量比阈值几乎无边际贡献（仅削减信号量）：

| 价格阈值 | 量比 1.5 | 量比 2.0 | 量比 3.0 | 量比 4.0 |
|---|---|---|---|---|
| 2.0 | +3.54% (n=1726) | +3.47% (n=1450) | +3.35% (n=1001) | +3.82% (n=695) |
| 2.5 | +4.35% (n=1338) | +4.18% (n=1158) | +3.82% (n=839) | +4.31% (n=601) |
| 3.0 | +5.38% (n=1041) | +5.13% (n=923) | +4.50% (n=692) | +4.61% (n=525) |
| 3.5 | +6.57% (n=837) | +6.04% (n=746) | +5.28% (n=573) | +5.39% (n=449) |
| **4.5** | **+8.10% (n=554)** | +6.66% (n=501) | +5.97% (n=404) | +6.30% (n=330) |
| 6.0 | +7.96% (n=359) | +7.41% (n=330) | +5.96% (n=273) | +6.43% (n=235) |

（PF 随价格阈值单调改善：3.0 档 ~1.8-2.0 → 4.5 档 ~2.0-2.3；日级 t 值 1.1~1.6，样本 regime 单一，待复校。）

**阈值敏感性结论（目标函数 = 高确定性优先）：**

1. **价格阈值是主杠杆，量比阈值不是**：收紧价格阈值期望与 PF 单调改善（3.0→4.5 档 24h 净均 +5.1%→+8.1%，PF 1.99→2.27）；量比从 1.5→4.0 期望基本持平，只在信号量大时轻微衰减。
2. **初版阈值建议**：`PRICE_THR_1H = 4.0`（3.5/4.5 之间取中，兼顾样本量与期望）、`VOL_RATIO_THR = 2.0`（保持现状，量比维度不作为质量过滤，仅作流动性护栏）。信号量约减少 40%（vs 3.0 档），符合"宁缺毋滥"。
   ⚠️ **口径边界（见 §12.1）**：本表是**单周期 1h** 口径——逐根判 `interval='1h'`
   单根涨跌幅，**没有 15m/5m 通道**。而线上 `_l1_screen` 是「三周期各自独立成通道、15m(2.0%) 也能单独入池」。
   故本表的「信号量约减少 40%」与 4.0 档期望，**只对单周期口径成立**；把线上 1h 改成 4.0 并不能复现这些效果
   （被 4.0 滤掉的「1h 涨 3~4%」样本在线上多数已被 15m 通道捕获）。同理，本表的 5m/15m 阈值**从未被标定**。
3. **窗口策略不变**：1h 窗口全档位期望 <0.6%（成本吃光），24h 为主窗口 → 警报语义为"24h 持有观察"，不做日内短打。
4. **实施**：`backtest_scan_scenarios.py --sweep` 作为复校工具，CVD 数据到位后重跑复核。

**复验机制：** `scan_oi_backfill` 每周维护 30 天 OI 窗口 + 实时 5m 采样持续积累；随窗口拉长定期重跑 `backtest_scan_scenarios.py` 复核结论稳定性，CVD 维待 WebSocket 流式落地后按同框架扩展。

---

## 9. 执行层衔接（P4，已落地 v1）

消费主池 high 置信 P↑OI↑ 多头信号，复用 `auto_trade_signal.py` 的风控/审计链路：

- **信号源**：`biz.scan_signal` 主池 high + **P↑OI↑**（`p_dir='up'` 且 `oi_dir='up'`）+ 已告警 + 未执行 + 未过期 + 最近 N 小时。取此口径的依据是回测中它唯一稳定正期望；空头侧在样本期内全负，故执行层只做多（⚠️ 样本为单边上涨 regime，见 §12.1-A2）。
  （实现 `phase_execute_scan_signal.py` 用 `p_dir='up' AND scenario IN ('S1','S2')` 表达同一集合——§4.3 的两套 `scenario` 口径下，`p_dir='up'` 的 S1/S2 都是 P↑OI↑，故等价。）
- **触发价**：盘面信号无自带进场价，以执行时现价为基准（limit 挂现价追单 / market 市价）。
- **止损**：`SignalSltpAdvisor` 三层结构——机械底线 max(ATR×2, 0.2%) → AI 定性 → 2% 权益硬顶 clamp；兜底止损立即挂防裸奔。
- **仓位**：按置信度分档（high=满额，medium=半额），单笔名义默认 300 USDT。
- **冷却**：同币 12h（查 `exec_state` + `executed_at` 窗口）。
- **安全**：默认 dry-run（只打印意图、不标记）；`--live` + `SIGNAL_TRADE_ENABLED=1` 双保险才下单并回填 `exec_state`。
- **审计**：`biz.scan_auto_trade_log`（每笔 dry-run/执行留痕）。
- **文件**：`scripts/bin/phase_execute_scan_signal.py`（迁移 `fix_046_scan_execute.sql`，已应用）；`--watch` 常驻或由 scheduler 周期触发。

```bash
python phase_execute_scan_signal.py                    # dry-run 扫待执行信号
python phase_execute_scan_signal.py --live             # 真实下单（需总开关）
python phase_execute_scan_signal.py --test-connection  # 自检币安连通性
```

---

## 10. 轧空行情扫描（Squeeze Scan，2026-09-21 新增）

与主池（§4）、蓄势池（§5）并列的**第三个检测池**：针对「价格快速拉升 + 空头被迫平仓」的轧空行情，
输出**行情性质判定**（落库）；邮件告警**当前处于影子模式**（只落库不发信，见 §10.8）。定位与 §1 一致——**只做数据监控与告警，不含任何下单逻辑**。

### 10.1 三阶段

| 阶段 | 内容 | 落点 |
|---|---|---|
| ① 轮询扫描 | 短窗口涨幅 + 放量初筛 → OI/爆仓/CVD 三重轧空确认 → 入跟踪队列 | `task_scan_squeeze` 阶段 1 |
| ② 跟踪 | 用实时价刷新峰值、计算回撤幅度、判到期 | 同上，阶段 2 |
| ③ 胜负判定 | 冲高回撤后，用**回撤窗口**的 OI/CVD/多空比/爆仓结构判定多空胜负 | 同上，阶段 3 |

> **核心口径（务必区分两个窗口）**：拉升阶段的 CVD 上涨是**空头平仓**（空头买回）造成的，
> **不能**用来判断多空胜负；胜负**只看冲高之后回撤窗口**的指标。这是本模块与 §4 主池最本质的差别。

### 10.2 数据源

| 数据 | 来源 | 关键限制 |
|---|---|---|
| 价格 / 涨幅 / 放量 | `biz.asset_klines`（5m、15m） | **阶段 1 初筛用 5m 收盘价**（`close_px`），**阶段 2 跟踪用 `_live_price`（REST 现价）**——两者不同源，`surge_pct` 与 `peak_px` 不可直接比 |
| OI / CVD | `biz.oi_cvd_snapshot`（5m，`source='realtime'`） | 缺桶时 `oi_chg`/`cvd_ratio` 为 `None`，走"缺失≠0"路径（见 §10.6） |
| 爆仓滚动窗口 | CoinGlass v4 `/api/futures/liquidation/coin-list` | HOBBYIST 套餐**粒度下限 4h**，无细粒度历史 → 只能 5min 轮询自建序列；实测 coin-list 刷新 20~40s 一次，5min 轮询不漏采；该套餐不含多空比（全系列 404）。**消费侧取滚动 1h 绝对值，不跨桶差分** |
| 爆仓细粒度历史（**未接入**） | CoinGlass `/api/futures/liquidation/history`（4h+，套餐可用） | 客户端已封装 `liquidation_history()` 但**线上无人调用** → 可作回测数据源 / 缺口补数（§12.1） |
| 大户/全局多空比、主动买卖比 | Binance 免费 `/futures/data/topLongShortPositionRatio`、`/topLongShortAccountRatio`、`/globalLongShortAccountRatio`、`/takerlongshortRatio` | period=5m、limit ≤ 500；**4 个端点时间戳互不相同**，必须逐 key 各取最近值（不能取「最新一条记录」）；**仅对 `tracking` 中的币按需拉取**（≤12 币/轮 × 4 端点） |

### 10.3 新增表（迁移 `fix_056_squeeze_scan.sql`）

| 表 | 主键 | 说明 |
|---|---|---|
| `biz.liquidation_snapshot` | (symbol, ts) | `liq_usd_1h/_4h/_12h/_24h`、`long_liq_usd_1h`、`short_liq_usd_1h`、`long_liq_usd_4h`、`short_liq_usd_4h`、`source`、`fetched_at` |
| `biz.long_short_ratio` | (symbol, period, ts) | `top_position_ratio`、`top_account_ratio`、`global_ratio`、`taker_ratio`、`fetched_at` |
| `biz.squeeze_track` | id | 跟踪队列：`status`、`surge_start_ts/px`、`surge_pct`、`peak_px/peak_ts`、`last_px/last_ts`、`retrace_pct`、`conclusion`、`reason`、`metrics JSONB`、`judged_at`、`expires_at`；**部分唯一索引** `uq_squeeze_track_active ON (symbol) WHERE status='tracking'`（同币同时只允许一条在跟） |
| `biz.scan_signal`（改） | — | 新增 `detail JSONB`，承载判定 metrics |

### 10.4 新增任务（均在 `scan_daemon.py` 内，supervisord 常驻）

| 任务 | 周期 | 错峰 | 说明 |
|---|---|---|---|
| `scan_liquidation` | 5min | +4min | CoinGlass coin-list → `liquidation_snapshot`（实测 1,597 币 → 映射 **527** 池内币；ts 取 5min 桶，与 OI 对齐） |
| `scan_squeeze` | 5min | +7min | 轧空 扫描/跟踪/判定 三阶段 |
| `prune_scan_data` | 24h | +10min | 保留期清理（**三表分档**）：`liquidation_snapshot` 30 天、`oi_cvd_snapshot` 90 天、`asset_klines` 5m/15m/1h = 90/180/730 天；`squeeze_track` 终态行与 `scan_signal` 因体量小且有回溯价值刻意不清理 |

**符号映射（易错点）**：CoinGlass 同时存在 `PEPE` 与 `1000PEPE`、`SHIB/1000SHIB`、`BONK/1000BONK`、`FLOKI/1000FLOKI`，
必须**精确码优先**（`alias_bases()` 返回 `[基码, 去1000后]` 且用 `setdefault` 写入），否则 `1000PEPEUSDT` 会抢走 `PEPE` 的数据。

> **中文名合约实测可用（2026-09-21）**：池内有 `龙虾USDT`/`哈基米USDT`/`币安人生USDT`/`我踏马来了USDT`/`牛来USDT`
> 等非 ASCII 符号，`base_symbol()` 只剥 `USDT/USDC/BUSD/USD` 后缀、不假设 ASCII，CoinGlass 侧**能对上同名 symbol**
> （实测 `龙虾USDT` 在 `liquidation_snapshot` 30 个桶全覆盖），故无需额外映射规则——但下游若要接 `core.asset` 需另做映射。

### 10.5 阈值常量（`crypto_research.analysis.squeeze`）

| 常量 | 值 | 含义 |
|---|---|---|
| `SURGE_THR_5M` / `SURGE_THR_15M` | 2.0% / 3.5% | 拉升初筛涨幅（5m / 15m） |
| `SURGE_VOL_RATIO_MIN` | 1.5 | 拉升初筛放量倍数（vs 前 20 根均量） |
| `OI_SHORT_COVER_PCT` | -0.3% | 轧空确认：OI 变化上限（价升 OI 降 = 空头回补特征） |
| `SQZ_SHORT_LIQ_RATIO_MIN` | 0.00008 | 轧空确认：空头爆仓额 / 24h 成交额 下限 |
| `SQZ_CVD_RATIO_MIN` | 0.02 | 轧空确认：cvd_ratio 下限 |
| `RETRACE_THR` | 2.0% | 触发判定的回撤幅度 |
| `MIN_OBSERVE_MIN` | 15 | 高点后最少观察时长（分钟） |
| `TRACK_EXPIRE_MIN` | 180 | 跟踪期上限（分钟），超时出队 |
| `TRACK_QUEUE_MAX` | 12 | 队列容量上限 |
| `SQUEEZE_COOLDOWN_H` | 6 | 同币判定后冷却（小时） |
| `OI_EXIT_PCT` | -1.0% | 回撤窗口 OI 变化 ≤ 此值 → 判定为「持仓快速离场」（否则视为持仓维持） |
| `CVD_BUY_MILD` / `CVD_SELL_STRONG` | +0.02 / -0.15 | 回撤窗口 cvd_ratio 分档（判定实际只用到这两个） |
| `LONG_LIQ_RATIO_THR` | 0.00040709 | 回撤窗口「多头大额爆仓」阈值（多头爆仓额 / 24h 成交额），与 `SQZ_SHORT_LIQ_RATIO_MIN` **独立常量、不联动**。⚠️ **临时值**：判据裕度为亚 pp 级、随口径与样本时段可翻盘 ⇒ 具体数字**不在文档留档**，一律以 `workbench/calib_squeeze_liq_thr.py` 当次实跑输出为准，待补齐 OI 采样后定稿（见 §12.1） |
| `MIN_WINDOW_COVERAGE` | 0.6 | **判定窗口 5m OI 桶覆盖率下限**，低于此值**放弃判定**（track 留 `tracking`，不落 `scan_signal`、不发邮件），stats 计入 `insufficient_coverage` |
| 尾部连续性（代码内联） | `oi_lag_sec > 2×300s` | 覆盖率只看「数量」，前段齐/尾部断会骗过闸门 ⇒ 再判「OI 最新桶滞后」；拒判 reason = `判定窗口尾部 OI 桶缺失…`，metrics 记 `oi_lag_sec`（工单 SQZ-03） |
| 窗口连续性闸门（`squeeze.window_gate` / `squeeze.MAX_MID_GAP_BUCKETS`） | 2 | 「前段齐、中段缺 1~2 桶、尾部齐」时覆盖率（数量）与尾部判据**都拦不住**，而重启/`os._exit(1)` 杀的恰是中间桶（快照不可回补）⇒ 独立判据：`[first_bucket, last_bucket)` 内最长连续缺桶 ≥2 即拒判，reason = `判定窗口内连续缺桶 N 个（左端起 h 个 / 中段 m 个…）`，metrics 记 `head_gap_buckets` + `mid_gap_buckets`（复验 P2-d / P3；右端正在采集的桶由尾部判据负责）。**左端单列**：`head_gap` 会让 `base_oi = _last_at_or_before(oi_sym, peak_ts)` 落到窗口之外、ΔOI 基准失真，语义比中段更重。实现已抽为**纯函数**并入单测（`test_squeeze_battle.py` C1~C10） |

> 全部阈值**跨币可比**：一律用「窗口金额 / 24h 成交额」的比例口径，不用绝对美元额（绝对额在小盘/大盘间不可比）。
>
> ⚠️ **`CVD_SELL_MILD = -0.08` 是死常量**：代码里有定义但 `evaluate_battle()` **从未使用**
> （判定实际只用 `CVD_SELL_STRONG` / `CVD_BUY_MILD` 两条线）。要么删、要么在判定里真正用起来——**不要以为它生效**。
>
> ⚠️ **阈值标定状态**：
> - `SQZ_SHORT_LIQ_RATIO_MIN = 0.00008` 的落点**不在文档留数字**（随数据集与分母口径漂移）⇒ 一律以
>   `workbench/calib_squeeze_liq_thr.py --days 1` 当次实跑的「含 0 / >0 双率 + 各自 n」为准（阈值本身未动）。
> - `LONG_LIQ_RATIO_THR = 0.00040709` 与它**必须保持独立常量**（语义相反、分布不同），不得写成同一引用；
>   该值仍属**临时**，待「进入判定的真实窗口」条件分布复核后定稿。
>
> ⚠️ **比例口径是混合源**：分子 = CoinGlass **全交易所**爆仓额，分母 = **币安** 24h 成交额 → 系统性偏高，仅作横向比较。

### 10.6 判定规则（`evaluate_battle`，互斥分支）

```text
big_long_liq = (long_liq_ratio is not None) and long_liq_ratio >= LONG_LIQ_RATIO_THR
d_oi_pct     = 回撤窗口 OI 变化率（%，基于 peak 时刻基准桶）
cvd_ratio    = 回撤窗口净主动成交 / 窗口成交额 ∈ [-1, 1]

# ── 前置闸门：覆盖率不足直接不判定（不落库、不发邮件）──
if 窗口 5m OI 桶覆盖率 < MIN_WINDOW_COVERAGE:  -> 留 tracking（stats.insufficient_coverage）

if   cvd_known and liq_known and cvd_ratio <= CVD_SELL_STRONG and big_long_liq:
                                                       -> short_win    # 多头踩踏
elif oi_known  and liq_known and d_oi_pct  <= OI_EXIT_PCT and not big_long_liq:
                                                       -> profit_take  # 多头止盈离场/趋势衰竭
elif oi_known  and cvd_known and liq_known and d_oi_pct > OI_EXIT_PCT \
     and cvd_ratio >= CVD_BUY_MILD and not big_long_liq:
                                                       -> long_win     # 新多承接
       （若 top_ratio_chg < 0，即大户持仓多空比下降 → 降级为 churn，疑似换手）
else:                                                 -> churn        # 震荡消化
```

> ⚠️ **「缺失 ≠ 为 0」策略（必须保留）**：三个关键输入
> `d_oi_pct` / `cvd_ratio` / `long_liq_ratio` 任一为 `None` 时：
> 1. **不得静默补 0 后照常判定**——补 0 会让"无爆仓数据"无条件满足 `not big_long_liq`，
>    从而把**缺数据误判成** `profit_take` / `long_win`；
> 2. 依赖该维度的分支一律**不成立**：`long_liq_ratio is None` 时禁止 `profit_take` / `long_win`
>    （这两条的前提是「**确知**无多单踩踏」）；`short_win` 需 `cvd` + `long_liq` 已确知；
> 3. 缺失维度写进 `metrics['data_missing']`、文案显式标注、**降级为 `churn`（无方向）**，不新增结论枚举。

四个结论与邮件标签：

| conclusion | 标签 | 含义 |
|---|---|---|
| `long_win` | 多头胜（新多承接） | 持仓维持 + 主动买占优，新多头接住了抛压 |
| `short_win` | 空头胜（多头踩踏） | 主动卖强 + 大额多头爆仓 |
| `profit_take` | 多头止盈离场（趋势衰竭） | OI 快速下降（多头离场）且无多头爆仓 |
| `churn` | 多空平局（震荡消化） | 其余（含大户多空比下降的疑似换手） |

方向判据来自**大户持仓多空比变化 `top_ratio_chg`** 与**主动买卖比 `taker_ratio`**；
**OI 只承担「持仓是维持还是离场」这一件事**，不承担方向。

> ⚠️ **方向判据的冷启动与基准口径（待验证，2026-09-21 实测）**：`biz.long_short_ratio` 目前只有 **21 行 / 1 币**
> （仅 NEARUSDT，样本 105 分钟）——因为该表**只对入队后的跟踪币采集**。由此带来两个风险：
> 1. **基准桶可能取错**：入队时拉的是 `limit=20`（≈100min），若**峰值出现在入队之前**（拉升走完才被扫到），
>    `top_ratio_chg` 的基准就落在错误位置 → 方向判据失真。建议入队时回拉 `limit=100`（≈8h），
>    或显式把「基准桶 = peak 桶」写进判定口径。
> 2. **维度缺失会被降级**：拉不到多空比时 `top_ratio_chg = None`，按 §10.6 原则不阻止判定，
>    但方向证据只剩 `cvd_ratio`，结论更容易落到 `churn`（如实测 NEARUSDT 一例即 `churn`）。

### 10.7 对原始伪代码的 6 处修正（本模块的设计依据）

| # | 原伪代码问题 | 修正 |
|---|---|---|
| 1 | 引用了未定义的 `CVD_peak`，且混用拉升期与回撤期的 CVD | 显式区分「拉升阶段 CVD 增量」与「回撤窗口 CVD 增量」，**胜负只用后者** |
| 2 | 用累计 CVD 的绝对量作比例基准，跨时段不可比 | 归一化为 `cvd_ratio = 窗口净主动成交 / 窗口成交额 ∈ [-1,1]`，跨币跨时段可比 |
| 3 | 分支 1/2 的 OI 条件完全相同，无法区分方向 | 引入大户持仓多空比 / 主动买卖比作**方向判据**，OI 仅承担「维持还是离场」 |
| 4 | `MIN_LIQ` 等绝对金额阈值无定义且跨币不可比 | 一律改为「窗口爆仓额 / 24h 成交额」比例 |
| 5 | 分支 3（OI 快速下降）语义被写成「平局」 | 更正为 `profit_take`（多头止盈离场 / 趋势衰竭），与 `churn` 分离 |
| 6 | 判定只在首次回撤 2% 时触发一次，缺少观察期约束 | 增加「高点后最小观察时长」`MIN_OBSERVE_MIN` 与「跟踪期上限」`TRACK_EXPIRE_MIN` |

### 10.8 落库与告警

- 判定结果写 `biz.squeeze_track`（`status`/`conclusion`/`reason`/`metrics`/`judged_at`）
- 同时写 `biz.scan_signal`：`pool='squeeze'`、`scenario='SQZ_<CONCLUSION>'`、`status='confirmed'`、`detail` = metrics JSONB
- 邮件告警（**当前处于影子模式**，`SQUEEZE_ALERT_SHADOW = True`）：判定照常入队/跟踪/落库，但**不发邮件**，`stats` 记 `shadow` 条数。降影子的原因是判定阈值标定在极小且单一 regime 样本上、样本内选参、无 holdout（见 §12.1-A1/A3）。
  - ⚠️ 影子期**刻意不回填** `scan_signal.alerted_at`：该列同时是 §6 跨池互斥（`_cross_pool_recent(..., ("squeeze",))`）的判据，影子期既然不发信，就不该让主池因一封并不存在的邮件被静音。
  - 恢复发送的形态（口径验证通过后启用）：每轮把本轮判定出的标的合成一封 HTML 邮件（**只发一次，不做 12h 冷却**），发送成功后回填 `scan_signal.alerted_at`。
- **无任何下单逻辑**（与 §9 执行层完全解耦）

### 10.9 上线实测（2026-09-21，prod 只读查询）

- `scan_liquidation`：**有产出但非"无缺口"**。实测 02:00–06:05 UTC 共 **30 个 5min 桶**（理论约 50 个，即 ~60%），
  逐小时 6 / 11 / 7 / 4 / 2；缺口含 02:05–02:25、03:40、04:00、04:40–05:25、05:40、05:50。
  近 6h 桶覆盖：5m K 线 **72/72**、OI 实时 **41/72**、爆仓 **30/72**。
  - ⚠️ **这不是缺陷，是设计约束下的正常形态**：CoinGlass 给的是**滚动窗口绝对值**，每行自包含
    （`liq_usd_1h` 就是"最近 1 小时爆仓额"），**缺桶不会污染指标**，无须回填；消费侧取「最近一条 ≤ 容差」的快照即可。
  - 唯一的真实风险：若某币的爆仓极值**恰好落在缺口内**，入队确认（阶段 1）会漏掉它。缓解手段是放宽快照容差
    或改用 4h 滚动列兜底（见 §12.1）。
- `scan_squeeze`：单轮 `{'universe': 217, 'scanned': 167, 'surge': 0, ...}`，耗时 5.9s
  → 再一次印证**阶段 1 是低频事件**（三重条件同时满足）。累计实测只入队 2 次、判定 1 次（NEARUSDT，`churn`）。
- 事务内回滚冒烟（注入 BTCUSDT 合成跟踪行）：判定链贯通 → `conclusion='churn'`、`metrics` 完整、
  `scan_signal` 正确写入，prod 零残留。
- `prune_scan_data`：边界校验通过（31 天前合成行被删、29 天前保留）。

> **观察项 ①（阶段 1 阈值）**：三重条件同时满足的频率极低（近 5h 仅 2 次入队、`surge` 长期为 0）。
> 建议把判定规则写死：**若 48h 内 `surge` 累计为 0，则下调 `SURGE_THR_5M`（2.0%→1.5%）或 `SURGE_VOL_RATIO_MIN`（1.5→1.2）**，
> 而不是"再等等看"。
> **观察项 ②（爆仓阈值标定）**：两个爆仓阈值均**保持当前值不动**（`SQZ_SHORT_LIQ_RATIO_MIN = 0.00008`、`LONG_LIQ_RATIO_THR = 0.00040709`），
> 待补齐 OI 采样与标定脚本口径后定稿；落点与裕度**不在文档留数字**，以 `calib_squeeze_liq_thr.py` 实跑为准（见 §10.5）。

### 10.10 拉升期结构分解与轧空衰竭判定（**影子模式**）

§10.1 的三阶段回答「**冲高回撤后**多空谁赢了」；本节回答另一个问题——「**拉升期**这波由什么驱动、轧空的弹药还剩多少」。两者**并存**：本节挂在阶段 2 的跟踪循环里，**不替换** §10.6 的 `evaluate_battle`。

**交付形态（当前）**：`status='tracking'` 期间**每轮评估**（时效性即价值：行情还在拉升或刚见顶时就要能提示），结果落 `biz.squeeze_track.metrics['fuel']`。**影子模式、不发信**：阈值全为未标定初值、`dL`/`dS` 与买平/卖平均为代理口径（见 §10.10.8）。

**开信后的形态（待 §10.10.8 三项待办完成）**：独立早警邮件（`pool='squeeze'`、`scenario='SQZ_FUEL_<VERDICT>'`）、达标即发。⚠️ 与 §10.8 的判定邮件**解耦**，同一币可能先后收到两封 ⇒ 必须并入 §6 的跨池互斥口径（`CROSS_POOL_MUTE_MIN`），不能各发各的。

#### 10.10.1 数据可用性（对照需求的 6 项）

| # | 需求项 | 现有可得性 | 结论 |
|---|---|---|---|
| 1 | 总持仓 OI | `biz.oi_cvd_snapshot.oi_usd`（5m、`source='realtime'`） | ✅ 真值 |
| 2 | 大户多空持仓比 | `biz.long_short_ratio.top_position_ratio`（5m，仅 tracking 币按需采集） | ✅ 真值（受冷启动约束，见 §12.1） |
| 3 | 大户多仓/空仓**绝对量** | ❌ 无任何可用源给出绝对名义额 | ⚠️ 代理 |
| 4 | 主动平仓量/主动开仓量 | ❌ 无 open/close 标记 | ⚠️ 代理 |
| 5 | CVD | `biz.oi_cvd_snapshot.cvd_5m_usd`（aggTrades 自算） | ✅ 真值 |
| 6 | 空头爆仓金额 | `biz.liquidation_snapshot.short_liq_usd_1h`（CoinGlass 滚动 1h 绝对值） | ✅ 真值（滚动窗口，非分段增量） |

⚠️ **第 3、4 项无真值源，且与套餐档位无关**：

- **第 3 项**：Binance 免费端点 `/futures/data/topLongShortPositionRatio` 只返回 `longAccount`/`shortAccount`（**占比**，和为 1），无绝对名义额；CoinGlass **多空比全系列在 HOBBYIST 下 404**（客户端 docstring 已实测记录），**升级套餐拿到的也只是与 Binance 同源的比值**，仍非绝对量。
- **第 4 项**：`aggTrades` 的 `isBuyerMaker` 只标记**谁是主动方**，**不区分开仓/平仓**；`takerlongshortRatio` 只给主动买/卖**量比**。HOBBYIST 可用的 `taker-buy-sell-volume/history` 最小粒度 **4h**、且同样是主动买/卖量而非开/平分类 ⇒ 对分钟级轧空判定不可用。
- ⇒ **代理口径是本方案唯一可行路径**，邮件必须显式标注（见 10.10.6）。

#### 10.10.2 两个代理口径的推导（均**无需改表**）

**① 大户多/空仓分解**：占比可由已存比值反推，故 `biz.long_short_ratio` **不必新增列**：

```text
r          = top_position_ratio            （已落库）
longShare  = r / (1 + r)                   （= Binance longAccount）
shortShare = 1 / (1 + r)                   （= Binance shortAccount）
L_proxy    = oi_usd × longShare            （大户多仓代理量）
S_proxy    = oi_usd × shortShare           （大户空仓代理量）
```

⚠️ **代理量的语义边界**：它等于「OI × 占比」，只有在**「大户占 OI 的份额在窗口内稳定」**这一假设下，其**相对变化**才可读。它**不是**大户真实绝对仓位，跨币不可比 ⇒ 只做**同币同窗口内 `dL` / `dS` 的符号与相对幅度**判断。

**② 主动平仓/开仓**：用 `OI 变化 × CVD 方向` 四象限反推（免费数据下唯一路径）：

| | CVD > 0（主动买） | CVD < 0（主动卖） |
|---|---|---|
| **OI ↓** | 买平（空头回补）✅ 轧空特征 | 卖平（多头离场） |
| **OI ↑** | 新多开仓（假轧空） | 新空开仓 |

窗口内按 5m 桶分类后取占比 `btc_share`（buy-to-close 桶数 / 有效桶数）。

#### 10.10.3 判定规则（纯函数 `crypto_research.analysis.squeeze_fuel`）

窗口固定 `W = [surge_start_ts, now]`（与 §10.6 的 `[peak_ts, now]` **不同窗口，勿混用**）。

```text
dOI = OI_last / OI_first - 1                   （窗口首尾，非回撤窗口口径）
dL  = L_proxy_last / L_proxy_first - 1
dS  = S_proxy_last / S_proxy_first - 1
dR  = r_last - r_first                         （比值变化，仅作展示）
liq_peak  = max(short_liq_usd_1h within W)
liq_decay = short_liq_usd_1h_last / liq_peak    （<1 即萎缩；liq_peak 未越阈时该量无意义）
cvd_divergence = 价格在 W 内创新高，但累计 CVD 未创新高
cvd_ratio_W    = Σ cvd_5m_usd / Σ vol_5m_usd

# ── 前置闸门（10.10.5）：不过 → mixed，记 data_missing，不出方向 ──

if   dOI >= +OI_UP_THR   and dS >= +SHORT_ADD_THR:            -> short_rebuild
elif dOI >= +OI_UP_THR   and dL >= +LONG_ADD_THR:             -> long_pump
elif dOI <= -OI_DOWN_THR and dS <= -SHORT_CUT_THR and dL >= -LONG_FLAT_THR:
        if liq_peak >= SQZ_SHORT_LIQ_RATIO_MIN and liq_decay <= LIQ_DECAY_THR \
           and cvd_divergence:                                -> sqz_fuel_exhausting
        else:                                                 -> sqz_fuel_active
else:                                                         -> mixed
```

优先级即书写顺序（**先判 OI 升**的两类，再判 OI 降的两类，`mixed` 兜底）——这样「多头主动开仓拉盘」这个假信号在结构上不可能落进 `sqz_fuel_*`。

**结论语义与邮件标签**：

| verdict | 标签 | 含义 |
|---|---|---|
| `sqz_fuel_exhausting` | **轧空弹药耗尽** | 空头持续平仓止损推高比值 + OI 净降（无新增空头对手盘）+ 空头爆仓萎缩 + CVD 背离 |
| `sqz_fuel_active` | 轧空进行中（弹药尚存） | 同为平仓驱动，但空头爆仓仍在高位 |
| `long_pump` | 多头主动开仓拉盘 | OI 升 + 大户多仓代理升、空仓不降 ⇒ **比值上升非空头平仓所致，不是轧空** |
| `short_rebuild` | 新增空头进场 | OI 升 + 大户空仓代理升 ⇒ 轧空被承接 |
| `mixed` | 结构不明 | 其余（含数据缺失降级） |

> ⚠️ 需求点名的假信号「大户大量开多、空单不动 → 比值也涨」由 `long_pump` 单列承接；这正是「**必须同时看多仓/空仓两个序列、不能只看比值**」的落地点。
>
> ⚠️ **证据强度**：`dL` / `dS` 与「买平/卖平」均来自 §10.10.2 的**代理口径**（非真值源），其成立前提（大户占 OI 份额在窗口内稳定、四象限可代表开平仓）**尚未验证**（见 §12.1-C6）。故本节结论的强度低于真值源，邮件必须按 10.10.6 显式标注口径。

#### 10.10.4 阈值常量（`analysis/squeeze_fuel.py`，**全为经验初值**）

| 常量 | 初值 | 含义 |
|---|---|---|
| `OI_UP_THR` / `OI_DOWN_THR` | +0.3% / -0.3% | 窗口 OI 净增/净减分界 |
| `SHORT_ADD_THR` / `SHORT_CUT_THR` | +3% / -3% | 大户空仓代理量增减分界 |
| `LONG_ADD_THR` / `LONG_FLAT_THR` | +3% / -1% | 大户多仓代理量「显著加仓」/「基本持平」分界 |
| `LIQ_DECAY_THR` | 0.5 | 空头爆仓衰减比上限（当前 / 窗口峰值 ≤ 此值视为萎缩） |
| `MIN_LSR_POINTS` | 6 | W 内 LSR 有效点数下限（≈30min），不足拒判 |
| `MIN_FUEL_BUCKETS` | 3 | W 内有效 5m 桶数下限 |

⚠️ 与 §10.5 同纪律：**取值不在本文档留数字结论，一律以标定脚本当次实跑为准**（脚本另立，见 10.10.8）。

#### 10.10.5 闸门（**复用 §10.5，不新造口径**）

| 闸门 | 来源 | 不过时 |
|---|---|---|
| 缺失 ≠ 0 | §10.6 既有策略 | 依赖该维度的分支不成立，降级 `mixed` + `data_missing` |
| 窗口覆盖率 ≥ `MIN_WINDOW_COVERAGE` | §10.5 | 拒判（留 `tracking`、不发信） |
| 尾部 `oi_lag_sec ≤ 2×300s` | §10.5 / SQZ-03 | 拒判 |
| 连续缺桶 < `MAX_MID_GAP_BUCKETS`（`window_gate`） | §10.5 复验 P2-d / P3 | 拒判 |
| W 内 LSR 点数 ≥ `MIN_LSR_POINTS` | 本节新增（冷启动） | 拒判 |
| `liq_peak` 必须越阈 | 本节新增 | 「萎缩」无意义 ⇒ 不得判 `sqz_fuel_exhausting` |

⚠️ **「衰减必须先有峰值」是本节最易写错的一条**：若 W 内空头爆仓从未越过 `SQZ_SHORT_LIQ_RATIO_MIN`，`liq_decay` 是一个无意义的比值（分子分母都很小），照它判会得出「爆仓萎缩 ⇒ 弹药耗尽」的假结论——实际是「**爆仓从未发生**」。

#### 10.10.6 邮件文案模板

```text
空头在持续平仓止损，推高大户多空持仓比（{r_first:.3f} → {r_last:.3f}）；
但整体 OI {dOI:+.1f}% 持续下降，无新增空头对手盘进场。
窗口内空头爆仓由 {liq_peak} 降至 {liq_now}（衰减至 {liq_decay:.2f}），
主动成交 {btc_share:.0%} 为买平（空头回补）。价格新高但 CVD 未新高，
现货无新增买盘承接。轧空弹药正在耗尽。

⚠️ 口径：大户多/空仓为 OI × 占比 的代理量（非真实绝对仓位）；
买平/开仓由 OI × CVD 四象限反推，非交易所开/平标记。
```

#### 10.10.7 落地实现（三步，已落地）

1. `scan_daemon._fetch_long_short_ratio` 入队改 `limit=100`（≈8h）——顺带解基准桶取错问题（见 §12.1）；**占比不落库**（`longShare = r/(1+r)` 可反推）⇒ `fix_056` 表结构**无需变更**。
2. 纯函数 `analysis/squeeze_fuel.py`（`evaluate_fuel` / `classify_fuel` / `fuel_gate` + 两个代理口径 + 阈值常量）+ 单测 `workbench/test_squeeze_fuel.py`（纯函数注入风格，不依赖 DB / 网络）。
3. `task_scan_squeeze` 阶段 2 **每轮调用**，**影子模式**：只写 `squeeze_track.metrics.fuel`（用 `metrics || %s` **合并**语义，不覆盖入场指标），**不发邮件**。

⚠️ 判定闸门与燃料闸门**各自独立**（窗口不同、口径不同）：判定窗口残缺不阻碍燃料评估，反之亦然 ⇒ 两条路径都落 `fuel`（拒判时 `verdict=None` + `gate_reason`，与 `mixed` 的「结构不明」区分开）。

#### 10.10.8 为什么不能先回测标定

`biz.long_short_ratio` **只对 tracking 币采集**且历史极短（实测 **21 行 / 1 币**），`liquidation_snapshot` 自 09-16 起、HOBBYIST 又无细粒度历史 ⇒ **没有可用于标定的历史样本**，只能前向积累。与 §12.1 的既有结论一致（阈值标定一律以脚本实跑为准，不在文档留数字）。

**待办**：① 标定脚本 `workbench/calib_squeeze_fuel_thr.py`（口径同 `calib_squeeze_liq_thr.py`：分母自证 + `denominator_ok` + `exit 3`）；② `SQZ_FUEL_*` 与 §10.8 判定邮件的跨池互斥（同 §6 口径，见 §12.1）；③ 影子模式观察期长度与开信门槛。

---

## 11. 实施路线图

| 阶段 | 内容 | 产出 | 状态 |
|---|---|---|---|
| P0 数据基础 | 建新表；OI/CVD 采样任务；爆仓采集任务；K 线拉取任务 | 数据自积累启动 | ✅ 已完成（2026-09-16）。⚠️ 其中「清算事件订阅（WS）」通路**实测 0 行**，已在 `supervisord.conf` 停用该进程（脚本与表保留，见 §3.2 ②′） |
| P1 回测标定 | 数据回填 → 8 场景回测 → 场景标定表 | 置信度分级定稿 | 🚧 三轮标定已出，置信度已按结果修订并在代码中落地（§4.3）；⚠️ 样本内选参、单一 regime、t 值偏弱，**尚无 holdout**（§12.1-A1/A3） |
| P2 主池 | L0 环境过滤 + L1 粗筛 + L2 校验 + 信号落库 | 主池上线 | ✅ **已上线（实测）**：累计 main 池信号 148 条（S1 79 / S7 13 / S4 16 / S8 8 / S2 13 / S5 5 / S6 4 / S3 8），含 53 条 high，已告警 46 条。⚠️ 其中 **S5~S8 共 30 条来自早期独立脚本 `phase_scan_main_pool.py`**（S1~S4 = 113 条）；**现役 daemon 只产 S1~S4**，见 §4.3 ① |
| P3 蓄势池 | 蓄势判定 + 转主池联动 | 双池并行 | ✅ **已上线（实测）**：累计 ACC 807 条，由 `expire_signals` 按 7 天 TTL 收敛（见 §6.3）；ACC 冷却 `ACC_COOLDOWN_H=24` 已生效 |
| P4 执行衔接（可选） | 触发价/止损/仓位 + dry-run 链路 | 完整闭环 | ✅ v1 已落地（dry-run 验证通过；live 待子账户 API Key 配置后启用）。⚠️ 实测 `exec_state` 全为 NULL，即**从未真实执行过**；候选集依赖 `status='active'` + `expired_at` |
| P5 轧空扫描 | CoinGlass 爆仓轮询 + Binance 多空比 + 扫描/跟踪/胜负判定三阶段 + 拉升期结构分解（§10.10） | 第三个检测池（判定落库；**邮件告警影子模式**，不下单） | 🚧 三阶段已落地（详见 §10）；⚠️ 阶段 1 阈值待样本复验、判定阈值无 holdout ⇒ 告警降影子（§10.8、§12.1-A1/A3）。§10.10 燃料判定已接线但**只落 `metrics.fuel`、不发信**（阈值未标定、代理口径未验证） |

**交付物与迁移清单（截至 2026-09-21 实测）：**

| 交付物 | 文件 / 对象 |
|---|---|
| 数据表迁移（asset_klines / oi_cvd_snapshot / scan_sampler_state / liquidation_events / scan_signal） | `scripts/migrations/fix_043_scan_data_tables.sql` |
| 告警表与去重键 | `fix_044_scan_alert.sql`、`fix_050_scan_stall_dedup.sql`、`fix_058_drop_legacy_stall_key.sql` |
| 资金费率历史 | `fix_045_funding_hist.sql` |
| 执行层字段（`exec_state` 等） | `fix_046_scan_execute.sql` |
| 新鲜度 / 心跳 / 数据来源 | `fix_049_scan_freshness.sql`、`fix_057_scan_heartbeat_source.sql`、`fix_059_oi_cvd_source_backfill.sql`（回标 111,593 行 `realtime`→`backfill`） |
| 轧空扫描表（squeeze_track / liquidation_snapshot / long_short_ratio） | `fix_056_squeeze_scan.sql` |
| 通用迁移执行器 | `scripts/apply_migration.py` |
| K 线采集（增量 + `--backfill-days`） | `scripts/bin/phase_scan_klines.py`（**已迁入 daemon 调度**） |
| OI/CVD 5m 采样（游标增量 CVD） | `scripts/bin/phase_oi_cvd_sampler.py`（**daemon 调度**） |
| OI 1h 历史回填（写 `source='backfill'`） | `scripts/bin/phase_backfill_oi_history.py`（scheduler 周任务 `scan_oi_backfill`） |
| 主池 / 蓄势池扫描 | **线上真源是 `scan_daemon.py` 的 `task_scan_main_pool` / `task_scan_accumulation`**；`phase_scan_main_pool.py` / `phase_scan_accumulation_pool.py` 为**独立脚本（未被 daemon 调度）**，前者保留完整 S1~S8 口径 |
| 双层告警 | 已并入 `scan_daemon.py::task_scan_alert`（原独立脚本 `scan_alert_monitor.py` 不再是运行入口） |

⚠️ `fix_050`（3 个）/ `fix_051`（2 个）/ `fix_056`（2 个）等编号在**不同子系统**各有同号迁移（催化剂方向闸门 / 信号分层等），引用时须带文件名而非只写编号（`fix_055` 目前只有催化剂侧 1 个）。

**数据回填进度（实测）：** `biz.asset_klines` 已覆盖 **286 个符号 × 5m/15m/1h 三周期**，1h 回溯至 **2026-06-18**；`biz.oi_cvd_snapshot` 为 realtime 64,166 行 + backfill 111,593 行。

采集侧已知限制：
- Binance fapi 偶发断连 → 已加连接复用 + 3 次重试。
- 回填并发过高触发 Binance IP 封禁（418）→ 已降至全局限频 3.3 req/s + 指数退避，并改为云端独占出口（§11.1）。
- CVD 单个采样窗口最多 2 页 aggTrades（约 2000 笔），超高频合约存在低估；后续可升级 WS 流式 CVD。
- 采集**允许缺桶**是常态而非异常：实测近 6h 覆盖 K 线 72/72、OI 41/72、爆仓 30/72，下游指标必须容忍（§7）。
- ⚠️ 清算事件通路整体失效（不是「平静无事件」）：`biz.liquidation_events` 至今 **0 行**（§3.2 ②′）。

### 11.1 实际部署形态

> 出口结论：本机出口 `66.90.98.106` 为 FDCservers 新加坡共享段，封禁由同 IP 邻居触发，节流无效，**唯一有效对策是独占出口** → 采集部署到云端美国节点（Zeabur / Tencent Ashburn）。
> 所有扫描任务收归常驻进程 `scan_daemon.py`（`workbench/scheduler.py` 仅保留低频辅助任务）。

**常驻进程 `scan_daemon.py`**（`supervisord.conf` → `[program:scan_daemon]`），单实例（`pg_try_advisory_lock`，带 TTL 可驱逐僵尸实例）内跑 **11 个线程任务**（`TASK_DEFS`）：

| 任务 | 周期 | 偏移 | 职责 |
|---|---|---|---|
| `scan_klines` | 300s | 0s | K 线增量（5m/15m/1h） |
| `scan_oi_cvd` | 300s | 120s | OI/CVD 5m 采样（写 `source='realtime'`） |
| `scan_liquidation` | 300s | 240s | CoinGlass 爆仓滚动窗口轮询 → `biz.liquidation_snapshot` |
| `scan_alert` | 300s | 180s | 双层告警（`NEW_WINDOW_MIN=20`、`COOLDOWN_H=12`） |
| `scan_main_pool` | 900s | 60s | 主池 L0+L1+L2 → `scan_signal`(pool=`main`) |
| `scan_accumulation` | 1800s | 0s | 蓄势池 ACC / BRK → `scan_signal`(pool=`accumulation`) |
| `scan_squeeze` | 300s | 420s | 轧空三阶段（入队 / 跟踪 / 判定） |
| `watchlist_monitor` | 1800s | 300s | 观察名单跟踪 |
| `expire_signals` | 1800s | 90s | **信号生命周期巡检**：超期 `active`/`confirmed` → `expired` + 写 `expired_at`（main/BRK 24h、ACC 7 天；见 §6.3） |
| `confirm_signals` | 1800s | 480s | **延续确认巡检**：`signal_ts` 后 6h 内一根**已收盘** 1h 收盘价越过 `breakout_px` ⇒ `active` → `confirmed`（含 2h 追补余量；见 §6.3） |
| `prune_scan_data` | 86400s | 600s | 保留期清理（**三表分档**：`liquidation_snapshot` 30 天 / `oi_cvd_snapshot` 90 天 / `asset_klines` 5m·15m·1h = 90·180·730 天） |

**仍留在 `workbench/scheduler.py` 的 4 个辅助任务**（低频 / 非实时）：

| 任务 | 频率 | 职责 |
|---|---|---|
| `scan_oi_backfill` | 周日 01:00 | OI 历史回填 |
| `scan_event_watchlist` | 每 6h | 事件观察名单刷新 |
| `scan_cvd_ready_check` | 03:05 | CVD 数据就绪检查 |
| `scan_freshness_watchdog` | 每小时 | 外部看门狗（独立进程校验 daemon 心跳） |

其余相关常驻进程：`[program:catalyst_fast_daemon]`（`--interval 900 --offset 120`，催化剂快通道）。⚠️ `[program:liquidation_collector]` **已注释停用**（0 行空转）。

**告警可观测性**：daemon 内置 `_stall_parts` 与外部看门狗 `check_scan_freshness.py` 的判据统一为 **`last_ok_at` + `last_error`**（不再只看 `last_run_at`），覆盖全 **11** 任务；`STALL_HEARTBEAT_TASKS` / `HEARTBEAT_MAX_AGE_MIN` 已补齐；`biz.scan_heartbeat` 含每任务心跳 + `__daemon__` 进程启动标记（冷启动宽限，避免启动即误报）；连续 3 轮失败 `os._exit(1)` 交 supervisord 重启。详见 AGENTS「静默失败根因修复」。

云端上线后建议执行出口配额的实测命令，读 `X-MBX-USED-WEIGHT-1M` 头确认配额占用正常。

---

## 12. 待决策问题与已知缺口

> 未决项与实现缺口统一收敛为 **§12.1**；已结案的历史待决策项（扫描范围=全量 Binance USDT 永续、采样频率=5m、8 场景收益口径、执行层纳入一期、CVD 数据源、冷却与有效期）不再重复。

### 12.1 已知缺口清单（按类别与风险排序）

**A 类 · 科学有效性（决定当前置信度分级与执行结论是否成立）**

- **A1 全系统参数标定在极小样本 + 单一 regime 上**：`confirm_signals` 的结论基于 **90 条 / 3 个交易日**、止损结论基于 **131 条 / 6 天**、OI 窗口仅 **21 天**；关键标定值 24h 净均 **+5.239%（日 t=1.23）**、P↑OI↑+负费率 **+15.582%（日 t=0.38）**——**t 值均 < 2**。参数是在同一份样本内选出来的（数据窥探），样本外表现未知。
- **A2 「空头默认降级、只做多」由单边上涨样本反推**：§4.3 的「空头侧默认降级」与 §9 的「只做多」依据是回测期内空头侧全负；样本期为单边上涨 regime ⇒ **不可外推为「空头侧无效」**。
- **A3 无样本外 / holdout 验证计划**：§8 三轮标定（含阈值敏感性网格选参）全为样本内。在补 holdout 之前，§4.3 的置信度分级与 §9 的选信号口径均属**待验证假设**。

**B 类 · 口径 / 标定（影响信号质量，但不会写错代码）**

- **B4 `scenario` 编号双口径**：同一 `S1..S8` 编号在两套编码里语义不同（§4.3 ①/②）。渲染层已改为按行自身维度重算标签、不再信任列内编号；但库内历史行仍混有两套口径，**按 `scenario` 直接聚合会混桶**（历史先验须按 `(p_dir, oi_dir)` 象限分组）。
- **B5 `oi_dir` 二值、无幅度门限**：`+0.06%` 与 `+50%` 同判 `up`，均可得 `high`。回测同为二值口径 ⇒ 目前不分档；要加分档需两侧同步改造并有回测支撑。
- **B6 线上最主要的入池通道（15m）从未被标定**：§8 阈值敏感性扫描是**单周期 1h** 口径，而线上 `_l1_screen` 还有 **15m(2.0%)** 独立通道 ⇒ 大部分入池由未标定通道决定，`PRICE_THR` 的 5m/15m 只有「经验初值」地位。15m K 线仅回溯至 2026-09-16，**当前不具备标定能力**。待办：历史积累到 ≥30 天后把回测改成与线上同构（三周期通道 + `level>=2`）重跑扫参。
- **B7 「升档共振」未实现，跨周期方向一致性缺失**：`_l1_screen` 各周期**独立**判定、取最高 `LEVEL_RANK`，**没有跨周期方向一致性检查**；`context_tags` 只携带触发那一个周期的标签。实际语义 = **「15m 或 1h 任一达标即入池」**，同一条 1h 级信号的 15m/5m 可能正好反向 ⇒「多周期共振提升质量」这一核心假设**从未被验证**（§8 回测同为单周期，无法证伪）。收紧为真共振会大幅削减信号量，需先有回测支撑。
- **B8 `asset_liquidity` / 买卖价差过滤未实现**：§7 只落地 `min_vol_usd=5e6`，价差与深度维度缺失，低流动性合约仍可能入池。
- **B9 `long_short_ratio` 冷启动基准**：`scan_squeeze` 入队只拉 `limit=20`（5m ⇒ 100 分钟），若**峰值早于入队时刻**，基准桶取错 ⇒ 方向判据失真。建议入队改拉 `limit=100`（见 §10.10.7）。
- **B10 初筛价与跟踪价不同源**：L1 粗筛用 K 线 `close_px`（入库有延迟），轧空跟踪用实时价，快速行情下两者背离，跨阶段比较需注意。
- **B11 爆仓阈值标定未定稿**：`SQZ_SHORT_LIQ_RATIO_MIN` / `LONG_LIQ_RATIO_THR` 均为**临时值**（两者独立、不联动）；落点与判据裕度**不在文档留数字**，一律以 `workbench/calib_squeeze_liq_thr.py` 实跑为准，待补齐 OI 采样后定稿。
- **B12 费率拥挤度未量化**：§4.3 的「持续大幅正/负费率」只有 `FUNDING_LABELS` 文本标签，无阈值判定，不参与打分。
- **B13 蓄势池阈值未标定**：`PRICE_QUIET_THR=1.5` / `VOL_CAP_RATIO=1.5` / `OI_HOURS=12` / `OI_RISE_RATIO=0.7` / `OI_CUM_CHG_PCT=5.0` / `BRK_VOL_RATIO=3.0` 均为初值，尚无回测标定。
- **B14 阶段 1 阈值过稀的观察项**：若连续 **48h** 无 surge 命中，需下调 `SURGE_THR_5M` / `SURGE_THR_15M`（当前 2.0 / 3.5）。
- **B15 催化层加分未落库**：§6 的催化层共振只在告警渲染时临时计算，不写回 `scan_signal` ⇒ 不影响信号强度与排序。
- **B16 爆仓细粒度历史未接入**：CoinGlass `liquidation/history` 最小粒度 **4h**（客户端已封装 `liquidation_history()`，线上无人调用），可改善爆仓维；需先定回填节奏与额度预算。
- **B17 重启丢桶 × 覆盖率闸门 = 拒判放大器**：`os._exit(1)` / 容器重启会杀掉飞行中的一轮，快照型表（OI/爆仓）对应 5m 桶**永久缺失**，`MIN_WINDOW_COVERAGE` 会把缺口转成拒判（只写 `reason`、无独立告警）。观测已加（`check_scan_freshness.py` 无条件评估近 2h 的 OI 桶数 + 尾部连续性 `oi_lag_sec`）；**根治（采样线程首轮补采已过去的桶）未做**。

**C 类 · 代理口径 / 覆盖 / 执行层 / 文档**

- **C6 代理口径的局限被低估**：§10.10.2 的两个代理（大户多/空仓绝对量 = `OI × 占比`；主动开/平仓 = `OI × CVD` 四象限）在 §10.10.3 被直接用作方向判据，其成立前提（大户占 OI 份额在窗口内稳定；四象限能代表开/平仓）**未被验证**，判定层也未按「真值 vs 代理」区分证据强度 ⇒ 结论强度应与真值源区分。
- **C7 新模块增速 > 标定数据积累**：`squeeze_fuel` 等新模块依赖的 `long_short_ratio` 历史极短（实测 **21 行 / 1 币**）、爆仓表自 2026-09-16 起积累 ⇒ 模块上线速度快于标定所需样本的积累速度，阈值只能前向积累。
- **C8 执行层与止损结论自相矛盾**：§9 执行层按「24h 持有观察」选信号，止损却用 `max(ATR×2, 0.2%)` 的短窗口机械底线 + 2% 权益硬顶，与 24h 持有语义不一致；且该止损依据的样本仅 **131 条 / 6 天**（见 A1）。
- **C9 数据覆盖与单点**：`COINGLASS_API_KEY` 缺失时 `scan_squeeze` 整轮失败（**密钥单点**）；KOL 共振维度仅覆盖 **3 个 `asset_id`**；爆仓维历史回测能力接近于零。
- **C10 文档与代码一致性无自动校验**：本轮已清理文档中所有被取代的表述，但文档与线上代码仍靠人工同步（B4 这类口径分叉即由此产生）⇒ 新增/修改参数时须同步文档与 `scan_daemon.py` 注释。
- **C11 主池告警静默丢失（补发未做）**：`_load_alert_candidates` 的 20min 窗口在 alert 任务停摆 >20min 时会把未告警的 high 信号**永久滤掉**（`alerted_at` 恒 NULL，与「超窗作废」不可区分）。只读检测已并入停摆邮件，**补发未做**（需先定义「补发有效性」边界）。
- **C12 主池邮件剩余缺口**：regime 三阈值中两个近乎死条件（`btc_1h` / `fgi` / `cap_trend`），收紧属标定决策；BRK 通道代码可达但 0 产出（保留分支、靠 stats 观测）；历史先验（中位/胜率/n）未落库。
- **C13 「标定结论已生效」类论断需逐条回查**：凡写「已按回测标定落地」的参数（阈值、权重、冷却），都需确认 ① 改的是**线上调度的那份代码**（`scan_daemon.py`）而非长年不跑的独立脚本；② 回测口径与线上口径**是同一个量**。

---

## 13. 风险与局限

- 免费数据源有 30 天 OI 窗口限制，长历史依赖自积累，回测样本前期受限。
- 清算历史缺失，空头侧场景回测只能近似验证（可接 CoinGlass `liquidation/history` 4h+ 缓解，见 §12.1）。
- 免费 API 限频（Binance `/futures/data/*` weight 限制），全市场高频采样需设计节流与分批；`scan_squeeze` 判定轮还会对 tracking 币（`TRACK_QUEUE_MAX=12`）按需拉价格与多空比，扩容该上限时须同步评估 weight。
- 所有信号均为概率性参考，需配合严格风控（仓位/止损/冷却）使用。

**CoinGlass 依赖项（2026-09-21 新增，本系统唯一付费数据源）：**

- **套餐粒度硬约束**：HOBBYIST 只支持 **4h 及以上**，1h 及以下返回 `code=403 + upgrade_required=STANDARD`；注意 **HTTP 状态始终 200**，业务错误只在 body 的 `code` 里，易被误判成网络故障。因此 **1h 爆仓只能靠 `liquidation/coin-list` 滚动快照高频轮询**——它是 S1 判定与轧空邮件数值的唯一来源。
- **滚动快照不可细粒度回测**：`coin-list` 是自包含快照而非历史序列，只能自 2026-09-21 起靠轮询积累；且**严禁跨桶差分**（差值是「新滚入 − 滚出」，平稳时≈0、回落为负，截断即假 0，见 §10.6）。这直接意味着**爆仓维的历史回测能力接近于零**，回测只能用代理指标或 4h+ 历史接口。
- **密钥单点**：`COINGLASS_API_KEY` 缺失时 `CoinGlassClient.__init__` 直接抛错，`scan_squeeze` 整轮失败。需确保告警链路能区分「密钥失效」与「无数据」两类成因。
- **口径混合**：比值分子为 CoinGlass **全交易所**爆仓额、分母为 Binance **24h 成交额**，属混合口径（§10.5 已披露，未做归一）。
- **限频与可用性**：实测约 1.3 req/s 连续 30 次无 429。当前 daemon 侧 CoinGlass 调用只有 `scan_liquidation` 每 300s 一次 `coin-list`，额度很宽松；未来若接入 4h+ `liquidation/history` 回填则需重新评估。
- 依赖 CoinGlass 服务可用性：其故障会导致轧空池全链停摆（主池 / 蓄势池不受影响）。
